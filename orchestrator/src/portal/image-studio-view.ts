import { escapeHtml } from './ui.js';
import { renderContentWorkspaceNavigation } from './content-workspace-navigation.js';
import type { ImageStudioGaugeView, ImageStudioView } from './image-studio-presenter.js';

const IMAGE_STUDIO_STYLE = `
  .imstudio{--im-bg:#050708;--im-panel:#0b0f11;--im-raised:#101619;--im-line:#253237;--im-line2:#3d4b50;--im-ink:#f3f7f6;--im-muted:#a5b2b4;--im-faint:#718185;--im-cyan:#00e5cc;--im-cyan2:#5ff6e6;--im-green:#72ebb0;--im-amber:#f0b84d;--im-red:#ff746b;min-width:0;overflow:hidden;border:1px solid #020303;background:var(--im-bg);color:var(--im-ink);font-family:var(--sans,ui-sans-serif,system-ui,sans-serif)}
  .imstudio *{box-sizing:border-box}.imstudio h1,.imstudio h2,.imstudio h3,.imstudio p{margin-top:0}.imstudio button,.imstudio textarea,.imstudio select,.imstudio input{font:inherit}.imstudio code{font-family:var(--mono,ui-monospace,monospace);overflow-wrap:anywhere}.imstudio a{text-decoration:none}
  .im-hero{position:relative;display:grid;grid-template-columns:minmax(0,1fr) minmax(310px,430px);gap:26px;align-items:end;padding:34px;border-bottom:1px solid var(--im-line);background:radial-gradient(circle at 88% 0,rgba(0,229,204,.2),transparent 31%),linear-gradient(130deg,#121a1d,#060809 66%);overflow:hidden}.im-hero:after{content:"";position:absolute;right:-120px;top:-230px;width:470px;height:470px;border:1px solid rgba(0,229,204,.14);border-radius:50%;box-shadow:0 0 0 55px rgba(0,229,204,.025),0 0 0 110px rgba(0,229,204,.018)}
  .im-kicker{position:relative;display:block;color:var(--im-cyan);font:900 11px/1.2 var(--mono,monospace);letter-spacing:.16em;text-transform:uppercase}.im-hero h1{position:relative;margin:10px 0 12px;font-family:var(--display,var(--sans));font-size:clamp(2.6rem,5.7vw,5.7rem);font-weight:580;line-height:.86;letter-spacing:-.06em}.im-hero h1 em{color:var(--im-cyan);font-style:normal}.im-hero-copy p{position:relative;max-width:820px;margin:0;color:var(--im-muted);font-size:14px;line-height:1.7}
  .im-model-card{position:relative;z-index:1;border:1px solid #33766e;background:rgba(3,18,17,.88);padding:17px}.im-model-card header{display:flex;align-items:center;justify-content:space-between;gap:12px}.im-model-card span{color:var(--im-cyan);font:900 11px var(--mono,monospace);letter-spacing:.08em;text-transform:uppercase}.im-model-card b{font-size:19px}.im-model-card p{margin:10px 0 0;color:var(--im-muted);font-size:12px;line-height:1.55}.im-model-card code{display:block;margin-top:10px;color:#6d9995;font-size:9px}
  .im-darkbar{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:13px;align-items:center;padding:11px 24px;border-bottom:1px solid var(--im-line);background:#090d0f}.im-darkbar strong{color:var(--im-red);font:900 11px var(--mono,monospace);letter-spacing:.09em;text-transform:uppercase}.im-darkbar p{margin:0;color:var(--im-muted);font-size:12px;line-height:1.5}.im-darkbar b{color:var(--im-ink)}.im-lock{border:1px solid #69433f;background:#1e0e0c;padding:5px 8px;color:#ffaaa4;font:850 10px var(--mono,monospace);text-transform:uppercase;white-space:nowrap}
  .im-gauges{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));border-bottom:1px solid var(--im-line);background:var(--im-panel)}.im-gauge-card{display:grid;grid-template-columns:112px minmax(0,1fr);gap:16px;align-items:center;padding:20px;border-right:1px solid var(--im-line)}.im-gauge-card:last-child{border-right:0}.im-gauge{--pct:0;position:relative;width:112px;aspect-ratio:1;border-radius:50%;display:grid;place-items:center;background:conic-gradient(from 225deg,var(--im-cyan) calc(var(--pct)*2.7deg),#253037 0 270deg,transparent 0);transform:rotate(-135deg)}.im-gauge:before{content:"";position:absolute;inset:10px;border-radius:50%;background:var(--im-panel);border:1px solid var(--im-line2)}.im-gauge-value{position:relative;z-index:1;display:grid;place-items:center;transform:rotate(135deg);font:900 21px var(--mono,monospace)}.im-gauge-value small{font-size:9px;color:var(--im-faint);text-transform:uppercase}.im-gauge-copy small{display:block;color:var(--im-faint);font:850 10px var(--mono,monospace);letter-spacing:.08em;text-transform:uppercase}.im-gauge-copy strong{display:block;margin:7px 0 5px;font-size:17px}.im-gauge-copy span{display:block;color:var(--im-muted);font-size:11px;line-height:1.45}
  .im-workspace{display:grid;grid-template-columns:minmax(360px,.92fr) minmax(420px,1.2fr) minmax(300px,.78fr);min-height:720px}.im-panel{min-width:0;border-right:1px solid var(--im-line);background:var(--im-panel)}.im-panel:last-child{border-right:0}.im-panel-head{min-height:75px;padding:15px 17px;border-bottom:1px solid var(--im-line)}.im-step{color:var(--im-cyan);font:850 10px var(--mono,monospace);letter-spacing:.1em;text-transform:uppercase}.im-panel-head h2{margin:5px 0 3px;font-size:16px}.im-panel-head p{margin:0;color:var(--im-faint);font-size:11px;line-height:1.45}
  .im-form{display:grid;gap:15px;padding:17px}.im-field{display:grid;gap:6px}.im-field label{display:flex;align-items:center;justify-content:space-between;gap:10px;color:var(--im-muted);font:800 11px var(--mono,monospace)}.im-field label span{color:var(--im-faint);font-size:9px;text-transform:uppercase}.im-input,.im-textarea,.im-select{width:100%;border:1px solid var(--im-line2);border-radius:6px;background:#070a0c;color:var(--im-ink);padding:10px 11px;line-height:1.5}.im-textarea{min-height:92px;resize:vertical}.im-input:focus-visible,.im-textarea:focus-visible,.im-select:focus-visible{outline:3px solid rgba(0,229,204,.23);border-color:var(--im-cyan)}.im-row{display:grid;grid-template-columns:1fr 1fr;gap:12px}.im-note{border-left:3px solid var(--im-cyan);background:#081513;padding:10px 11px;color:var(--im-muted);font-size:11px;line-height:1.55}.im-reference{display:grid;grid-template-columns:38px minmax(0,1fr) auto;gap:10px;align-items:center;border:1px solid var(--im-line);border-radius:7px;background:var(--im-raised);padding:9px}.im-reference-mark{width:38px;height:38px;display:grid;place-items:center;border:1px solid #347b73;border-radius:7px;color:var(--im-cyan);font:900 10px var(--mono,monospace)}.im-reference strong,.im-reference small{display:block}.im-reference strong{font-size:11px}.im-reference small{margin-top:3px;color:var(--im-faint);font-size:9px}.im-ok{color:var(--im-cyan);font:900 9px var(--mono,monospace);text-transform:uppercase}
  .im-stage{display:grid;place-items:center;min-height:480px;padding:26px;background:radial-gradient(circle at center,rgba(0,229,204,.06),transparent 44%),#070a0b}.im-canvas{position:relative;width:min(100%,510px);aspect-ratio:1;border:1px solid #385156;background:linear-gradient(145deg,rgba(0,229,204,.08),transparent 35%),repeating-linear-gradient(90deg,transparent 0 50px,rgba(255,255,255,.025) 51px),repeating-linear-gradient(0deg,transparent 0 50px,rgba(255,255,255,.025) 51px),#0a0f11;box-shadow:0 30px 80px rgba(0,0,0,.4);overflow:hidden}.im-canvas:before{content:"";position:absolute;left:19%;top:13%;width:58%;height:58%;border:2px solid rgba(0,229,204,.56);transform:rotate(11deg);clip-path:polygon(50% 0,100% 25%,90% 82%,52% 100%,9% 79%,0 26%)}.im-canvas:after{content:"";position:absolute;inset:8%;border:1px solid rgba(238,241,247,.18)}.im-canvas-copy{position:absolute;z-index:2;left:8%;right:8%;bottom:9%;padding:14px;border-left:3px solid var(--im-cyan);background:rgba(3,6,7,.82)}.im-canvas-copy span{display:block;color:var(--im-cyan);font:850 9px var(--mono,monospace);letter-spacing:.09em;text-transform:uppercase}.im-canvas-copy strong{display:block;margin-top:6px;font-size:15px;line-height:1.35}.im-preview-foot{display:grid;grid-template-columns:repeat(3,1fr);border-top:1px solid var(--im-line)}.im-preview-foot div{padding:10px;border-right:1px solid var(--im-line);font-size:9px;color:var(--im-faint)}.im-preview-foot div:last-child{border-right:0}.im-preview-foot strong{display:block;margin-top:3px;color:var(--im-muted);font-size:10px}
  .im-rail{padding:15px}.im-gate{border:1px solid #6f453f;border-top:3px solid var(--im-red);background:#170b0a;padding:13px}.im-gate strong{display:block;color:#ffaaa4;font-size:12px}.im-gate p{margin:7px 0 0;color:#c69e9b;font-size:11px;line-height:1.5}.im-btn{width:100%;min-height:48px;margin-top:11px;border:1px solid #4c5d61;border-radius:6px;background:#171e21;color:#69787b;font-weight:900;cursor:not-allowed}.im-rules{margin-top:14px}.im-rules h3,.im-queue h3{font-size:12px}.im-rules ul{display:grid;gap:7px;margin:0;padding:0;list-style:none}.im-rules li{display:flex;gap:8px;color:var(--im-muted);font-size:10px;line-height:1.45}.im-rules li:before{content:"×";color:var(--im-red);font-weight:900}.im-queue{margin-top:17px}.im-proposal{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;border-top:1px solid var(--im-line);padding:10px 0}.im-proposal strong,.im-proposal small{display:block}.im-proposal strong{font-size:11px}.im-proposal small{margin-top:4px;color:var(--im-faint);font-size:9px}.im-proposal code{color:var(--im-cyan);font-size:9px}.im-state{border:1px solid #6f5d31;padding:4px 6px;color:var(--im-amber);font:800 8px var(--mono,monospace);text-transform:uppercase;white-space:nowrap}
  .im-footer{display:flex;justify-content:space-between;gap:20px;padding:12px 18px;border-top:1px solid var(--im-line);color:var(--im-faint);font-size:10px}.im-footer strong{color:var(--im-muted)}
  @media(max-width:1150px){.im-workspace{grid-template-columns:minmax(320px,.9fr) minmax(420px,1.1fr)}.im-rail{grid-column:1/-1;border-top:1px solid var(--im-line)}.im-gauges{grid-template-columns:1fr}.im-gauge-card{border-right:0;border-bottom:1px solid var(--im-line)}}
  @media(max-width:760px){.im-hero{grid-template-columns:1fr;padding:24px 19px}.im-darkbar{grid-template-columns:1fr;padding:12px 18px}.im-lock{justify-self:start}.im-workspace{display:block}.im-panel{border-right:0;border-bottom:1px solid var(--im-line)}.im-gauge-card{grid-template-columns:92px minmax(0,1fr);padding:14px}.im-gauge{width:92px}.im-row{grid-template-columns:1fr}.im-stage{min-height:360px;padding:15px}.im-footer{display:grid}.im-hero h1{font-size:clamp(2.6rem,16vw,4.2rem)}}
  @media(forced-colors:active){.imstudio *{forced-color-adjust:auto}.im-gauge{background:Canvas;border:4px solid Highlight}.im-canvas{border:3px solid Highlight}}
`;

function gauge(
  view: ImageStudioGaugeView,
  value: string,
  detail: string,
  remainingLabel = `${view.remaining} remaining`,
): string {
  return `<article class="im-gauge-card"><div class="im-gauge" style="--pct:${view.percent}" role="img" aria-label="${escapeHtml(`${view.label}: ${view.used} of ${view.limit}`)}"><span class="im-gauge-value">${escapeHtml(value)}<small>${view.percent}% used</small></span></div><div class="im-gauge-copy"><small>${escapeHtml(view.label)}</small><strong>${escapeHtml(remainingLabel)}</strong><span>${escapeHtml(detail)}</span></div></article>`;
}

function references(view: ImageStudioView): string {
  return view.references.map((reference) => `<article class="im-reference"><span class="im-reference-mark" aria-hidden="true">${reference.kind === 'logo' ? 'PP' : 'ART'}</span><span><strong>${escapeHtml(reference.label)}</strong><small>v ${escapeHtml(reference.versionId.slice(0, 8))} · ${escapeHtml(reference.sha256.slice(0, 12))}…</small></span><span class="im-ok">${reference.approved ? 'verified' : 'blocked'}</span></article>`).join('');
}

function proposals(view: ImageStudioView): string {
  return view.proposals.map((proposal) => `<article class="im-proposal"><span><strong>${escapeHtml(proposal.label)}</strong><small>${escapeHtml(proposal.operation)} · $${(proposal.costMinor / 100).toFixed(2)} · ${escapeHtml(proposal.createdAt.slice(0, 16).replace('T', ' '))}</small><code>${escapeHtml(proposal.shortHash)}…</code></span><span class="im-state">${escapeHtml(proposal.state.replaceAll('-', ' '))}</span></article>`).join('');
}

export function renderImageStudioBody(view: ImageStudioView): string {
  const dayDetail = `${view.daily.used} used today from a hard allowance of ${view.daily.limit}.`;
  const concurrencyDetail = `${view.concurrency.used} active rehearsal slots; maximum ${view.concurrency.limit}.`;
  const spendDetail = `$${(view.spend.used / 100).toFixed(2)} of $${(view.spend.limit / 100).toFixed(2)} monthly ceiling.`;
  const effectLabel = !view.effects.providerEffectsEnabled
    ? `Effects OFF · emergency pause ${view.effects.emergencyPaused ? 'ON' : 'OFF'}`
    : view.effects.emergencyPaused
      ? 'Effects blocked · emergency pause ON'
      : view.effects.commandBoundaryAvailable
        ? 'Controlled proposal rail ready'
        : 'Provider policy ready · command boundary absent';
  const effectCopy = !view.effects.providerEffectsEnabled || view.effects.emergencyPaused
    ? 'The controls below cannot call OpenAI, spend money, publish artwork or reach an affiliate while the production switches remain dark.'
    : view.effects.commandBoundaryAvailable
      ? 'A generation request can create one review-only proposal within the displayed limits. Publishing remains a separate human decision.'
      : 'Provider policy is prepared, but this portal still has no executable command boundary.';
  const buttonLabel = view.generateAvailable ? 'Generate review proposal' : 'Generate review proposal · locked';
  return `${renderContentWorkspaceNavigation('images', { companyAssetsAvailable: true, composerAvailable: true, imageStudioAvailable: true, brandBrainAvailable: true })}<style data-property-predator-image-studio>${IMAGE_STUDIO_STYLE}</style><article class="imstudio" aria-labelledby="im-title" data-provider-effects="${view.effects.providerEffectsEnabled ? 'enabled' : 'none'}" data-generation-enabled="${view.effects.generationEnabled ? 'true' : 'false'}" data-command-boundary="${view.effects.commandBoundaryAvailable ? 'available' : 'absent'}"><header class="im-hero"><div class="im-hero-copy"><span class="im-kicker">Property Predator · Creative engine</span><h1 id="im-title">Image <em>Studio.</em></h1><p>Build premium, on-brand artwork briefs with ChatGPT’s strongest image model. The machine creates review-only proposals; the real Property Predator logo and any written copy are applied from approved company assets after generation.</p></div><aside class="im-model-card"><header><span>OpenAI image rail</span><b>${escapeHtml(view.model)}</b></header><p>Separate image-only credential. Exact Brand Brain and rules hashes. Bounded cost, volume and concurrency. Every output is inspected before it can enter approval.</p><code>${escapeHtml(view.credentialBoundary)}</code></aside></header><section class="im-darkbar" aria-label="Provider effect status"><strong>${escapeHtml(effectLabel)}</strong><p><b>${view.generateAvailable ? 'Controlled proposal mode.' : 'Safe rehearsal dashboard.'}</b> ${escapeHtml(effectCopy)}</p><span class="im-lock">fail-closed</span></section><section class="im-gauges" aria-label="Image engine limits">${gauge(view.daily, `${view.daily.used}/${view.daily.limit}`, dayDetail)}${gauge(view.concurrency, `${view.concurrency.used}/${view.concurrency.limit}`, concurrencyDetail)}${gauge(view.spend, `$${(view.spend.used / 100).toFixed(2)}`, spendDetail, `$${(view.spend.remaining / 100).toFixed(2)} remaining`)}</section><div class="im-workspace"><section class="im-panel" aria-labelledby="im-brief-title"><header class="im-panel-head"><span class="im-step">01 · Direct</span><h2 id="im-brief-title">Creative brief</h2><p>Structured intent only — no customer data, links or prompt overrides.</p></header><div class="im-form"><div class="im-field"><label for="im-subject">Subject <span>what exists in frame</span></label><input class="im-input" id="im-subject" value="${escapeHtml(view.brief.subject)}" autocomplete="off"></div><div class="im-field"><label for="im-concept">Forensic concept <span>single visual idea</span></label><textarea class="im-textarea" id="im-concept">${escapeHtml(view.brief.forensicConcept)}</textarea></div><div class="im-field"><label for="im-composition">Composition <span>brand-safe treatment</span></label><textarea class="im-textarea" id="im-composition">${escapeHtml(view.brief.composition)}</textarea></div><div class="im-row"><div class="im-field"><label for="im-size">Format</label><select class="im-select" id="im-size"><option selected>${escapeHtml(view.brief.size)}</option></select></div><div class="im-field"><label for="im-quality">Quality</label><select class="im-select" id="im-quality"><option selected>${escapeHtml(view.brief.quality)}</option></select></div></div><div class="im-note"><strong>Overlay discipline:</strong> generate the visual background clean. Apply the supplied real logo and written copy as deterministic artwork layers after approval — never ask a model to invent them.</div><div class="im-field"><label>Approved company references <span>exact hash required</span></label>${references(view)}</div></div></section><section class="im-panel" aria-labelledby="im-preview-title"><header class="im-panel-head"><span class="im-step">02 · Inspect</span><h2 id="im-preview-title">Proposal canvas</h2><p>Visual rehearsal, not a generated or provider-returned image.</p></header><div class="im-stage"><div class="im-canvas" role="img" aria-label="Fictional Property Predator image composition placeholder"><div class="im-canvas-copy"><span>Fictional composition preview</span><strong>${escapeHtml(view.brief.forensicConcept)}</strong></div></div></div><footer class="im-preview-foot"><div>Intended use<strong>${escapeHtml(view.brief.intendedUse)}</strong></div><div>Brand profile<strong>${escapeHtml(view.brand.profileSha256.slice(0, 12))}…</strong></div><div>Cost ceiling<strong>$${(view.brief.maximumCostMinor / 100).toFixed(2)}</strong></div></footer></section><aside class="im-panel im-rail" aria-labelledby="im-gate-title"><div class="im-gate"><strong id="im-gate-title">${escapeHtml(view.gateLabel)}</strong><p>${escapeHtml(view.gateDetail)}</p></div><button class="im-btn" type="button"${view.generateAvailable ? '' : ' disabled aria-disabled="true"'}>${escapeHtml(buttonLabel)}</button><section class="im-rules"><h3>Automatic brand rejection</h3><ul>${view.brand.forbidden.map((rule) => `<li>${escapeHtml(rule)}</li>`).join('')}</ul></section><section class="im-queue"><h3>Recent fictional proposals</h3>${proposals(view)}</section></aside></div><footer class="im-footer"><span><strong>${escapeHtml(view.workspaceName)}</strong> · fixture captured ${escapeHtml(view.capturedAt)}</span><span>No customer data · no publishing · no generated text or logo · human approval required</span></footer></article>`;
}
