/** Server-rendered, read-only Property Predator Operator Action Centre. */

import { escapeHtml } from './ui.js';
import type {
  OperatorActionCentreView,
  OperatorActionOwnerSummaryView,
  OperatorActionSourceSummaryView,
  OperatorActionView,
} from './operator-action-centre-presenter.js';

const OPERATOR_ACTION_CENTRE_STYLE = `
  .oac{--black:#050707;--night:#080b0c;--panel:#0e1315;--lift:#131a1d;--soft:#192226;--line:#243035;--line2:#34454c;--ink:#f5f7f6;--muted:#a8b3b6;--faint:#708087;--acid:#00e5cc;--acid2:#85ffec;--amber:#ffc45d;--red:#ff716a;--blue:#8ebaff;--violet:#c9a1ff;--mono:"SFMono-Regular",Consolas,"Liberation Mono",monospace;position:relative;isolation:isolate;min-width:0;overflow:hidden;border:1px solid #020303;background:var(--black);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}.oac *{box-sizing:border-box}.oac h1,.oac h2,.oac h3,.oac p{overflow-wrap:anywhere}.oac code{font-family:var(--mono);overflow-wrap:anywhere}.oac-visually-hidden{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important}
  .oac-hero{position:relative;display:grid;grid-template-columns:minmax(0,1.45fr) minmax(300px,.55fr);gap:30px;align-items:end;padding:38px 40px 30px;border-bottom:1px solid var(--line);background:radial-gradient(circle at 84% -5%,rgba(0,229,204,.19),transparent 31%),radial-gradient(circle at 11% 118%,rgba(142,186,255,.08),transparent 30%),linear-gradient(135deg,#11191b,#060808 68%);overflow:hidden}.oac-hero:before{content:"";position:absolute;right:29%;top:-202px;width:330px;height:330px;border:1px solid rgba(0,229,204,.12);transform:rotate(45deg)}.oac-hero:after{content:"ACTION / EVIDENCE / OWNER / SLA";position:absolute;right:31px;top:18px;color:rgba(0,229,204,.24);font:900 8px var(--mono);letter-spacing:.13em}.oac-kicker{position:relative;display:flex;align-items:center;gap:10px;color:var(--acid);font:900 10px/1.2 var(--mono);letter-spacing:.15em;text-transform:uppercase}.oac-kicker:before{content:"";width:31px;height:1px;background:var(--acid)}.oac-hero h1{position:relative;max-width:820px;margin:14px 0 14px;font-size:clamp(2.8rem,5vw,5.4rem);font-weight:620;line-height:.88;letter-spacing:-.06em}.oac-hero h1 em{color:var(--acid);font-style:normal}.oac-hero-copy>p{position:relative;max-width:760px;margin:0;color:var(--muted);font-size:14px;line-height:1.7}.oac-shift{position:relative;border:1px solid rgba(0,229,204,.3);background:rgba(5,8,9,.83);padding:18px 19px;box-shadow:0 24px 65px rgba(0,0,0,.32)}.oac-shift span{display:block;color:var(--faint);font:850 8px var(--mono);letter-spacing:.09em;text-transform:uppercase}.oac-shift strong{display:block;margin:7px 0 6px;font-size:15px}.oac-shift p{margin:0;color:var(--muted);font-size:10px;line-height:1.55}.oac-shift-meta{display:flex;gap:6px;flex-wrap:wrap;margin-top:13px}.oac-chip{display:inline-flex;align-items:center;min-height:27px;border:1px solid var(--line2);border-radius:999px;padding:4px 8px;color:var(--muted);font:850 8px var(--mono);letter-spacing:.04em}.oac-chip.env{border-color:#775b2a;color:var(--amber)}.oac-chip.good{border-color:#287b70;color:var(--acid)}.oac-chip.bad{border-color:#74403d;color:var(--red)}
  .oac-boundary{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:16px;align-items:center;padding:12px 40px;border-bottom:1px solid var(--line);background:#080c0d}.oac-boundary-mark{display:flex;align-items:center;gap:7px;color:var(--red);font:900 9px var(--mono);letter-spacing:.09em;text-transform:uppercase}.oac-boundary-mark:before{content:"";width:7px;height:7px;border-radius:50%;background:currentColor}.oac-boundary p{margin:0;color:var(--muted);font-size:10px;line-height:1.55}.oac-boundary p strong{color:var(--ink)}.oac-boundary-state{border:1px solid #69403e;padding:5px 8px;color:var(--red);font:900 8px var(--mono);letter-spacing:.06em;white-space:nowrap}
  .oac-headlines{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));border-bottom:1px solid var(--line);background:var(--panel)}.oac-headline{min-width:0;padding:17px 18px;border-right:1px solid var(--line)}.oac-headline:last-child{border-right:0}.oac-headline span{display:block;color:var(--faint);font:850 8px var(--mono);letter-spacing:.07em;text-transform:uppercase}.oac-headline strong{display:block;margin:6px 0 3px;font:900 23px var(--mono);letter-spacing:-.04em}.oac-headline small{display:block;color:var(--muted);font-size:8px;line-height:1.4}.oac-headline.hot strong{color:var(--red)}.oac-headline.warn strong{color:var(--amber)}.oac-headline.good strong{color:var(--acid)}
  .oac-pulse{padding:19px 24px 0}.oac-section-head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin:0 0 11px}.oac-section-head span{display:block;color:var(--acid);font:900 8px var(--mono);letter-spacing:.1em;text-transform:uppercase}.oac-section-head h2{margin:5px 0 0;font-size:16px}.oac-section-head p{max-width:590px;margin:0;color:var(--faint);font-size:9px;line-height:1.5;text-align:right}.oac-source-grid{display:grid;grid-template-columns:repeat(7,minmax(105px,1fr));gap:7px}.oac-source{position:relative;min-width:0;border:1px solid var(--line);background:var(--panel);padding:12px 12px 11px;overflow:hidden}.oac-source:after{content:"";position:absolute;left:0;bottom:0;width:var(--load);height:2px;background:var(--acid)}.oac-source-head{display:flex;justify-content:space-between;gap:8px;align-items:center}.oac-source-code{display:grid;place-items:center;width:30px;height:30px;border:1px solid #2d716a;background:#0b1d1c;color:var(--acid);font:900 8px var(--mono)}.oac-source-total{font:900 20px var(--mono)}.oac-source h3{margin:9px 0 3px;font-size:9px}.oac-source p{margin:0;color:var(--faint);font:800 7px var(--mono)}.oac-source p b{color:var(--red);font:inherit}
  .oac-layout{display:grid;grid-template-columns:minmax(0,1.7fr) minmax(300px,.62fr);gap:14px;padding:16px 24px 25px}.oac-main,.oac-side{display:grid;align-content:start;gap:14px;min-width:0}.oac-panel{min-width:0;overflow:hidden;border:1px solid var(--line);background:var(--panel)}.oac-panel-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;padding:15px 17px 13px;border-bottom:1px solid var(--line)}.oac-panel-head h2{margin:0;font-size:15px}.oac-panel-head p{margin:4px 0 0;color:var(--muted);font-size:9px;line-height:1.45}.oac-count{border:1px solid var(--line2);padding:5px 8px;color:var(--muted);font:850 8px var(--mono);white-space:nowrap}.oac-count.hot{border-color:#73413e;color:var(--red)}
  .oac-list{display:grid;gap:0}.oac-card{position:relative;display:grid;grid-template-columns:66px minmax(0,1fr);min-width:0;border-bottom:1px solid var(--line);background:var(--panel)}.oac-card:last-child{border-bottom:0}.oac-card:hover{background:#11181a}.oac-rank{display:flex;flex-direction:column;justify-content:space-between;align-items:center;gap:12px;padding:14px 9px;border-right:1px solid var(--line);background:#0a0e10}.oac-rank-index{color:var(--faint);font:900 8px var(--mono)}.oac-source-mark{display:grid;place-items:center;width:37px;height:37px;border:1px solid #2c716a;background:#0a1a19;color:var(--acid);font:900 8px var(--mono)}.oac-rank-line{width:1px;min-height:21px;flex:1;background:var(--line)}.oac-priority{font:900 8px var(--mono);color:var(--faint)}.oac-card.p0 .oac-priority,.oac-card.p0 .oac-sla{color:var(--red)}.oac-card.p1 .oac-priority{color:var(--amber)}.oac-card-body{min-width:0;padding:14px 15px}.oac-card-top{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.oac-eyebrow{display:flex;gap:6px;flex-wrap:wrap;align-items:center;margin-bottom:6px}.oac-eyebrow span{font:850 7px var(--mono);letter-spacing:.06em;text-transform:uppercase}.oac-source-label{color:var(--acid)}.oac-status{border:1px solid var(--line2);border-radius:999px;padding:3px 6px;color:var(--muted)!important}.oac-status.blocked{border-color:#75403d;color:var(--red)!important}.oac-status.waiting{border-color:#765e2d;color:var(--amber)!important}.oac-card h3{margin:0;font-size:13px;line-height:1.35}.oac-card-detail{margin:5px 0 0;color:var(--muted);font-size:9px;line-height:1.55}.oac-sla{flex:0 0 auto;border:1px solid var(--line2);padding:7px 8px;color:var(--muted);font:900 8px var(--mono);white-space:nowrap}.oac-sla.breached{border-color:#76413e;color:var(--red);background:rgba(255,113,106,.05)}.oac-sla.due_now{border-color:#7c5d2d;color:var(--amber);background:rgba(255,196,93,.05)}.oac-sla.due_today{border-color:#62532e;color:#e7ce86}.oac-card-grid{display:grid;grid-template-columns:minmax(0,1.1fr) minmax(0,.9fr);gap:8px;margin-top:11px}.oac-signal,.oac-owner{min-width:0;border:1px solid var(--line);background:var(--lift);padding:9px}.oac-signal span,.oac-owner span{display:block;color:var(--faint);font:800 7px var(--mono);text-transform:uppercase}.oac-signal strong,.oac-owner strong{display:block;margin-top:4px;color:var(--ink);font-size:9px;line-height:1.4}.oac-person{margin-top:4px!important;color:var(--blue)!important;font:800 8px var(--mono)!important}.oac-owner-inner{display:grid;grid-template-columns:29px minmax(0,1fr);gap:8px;align-items:center;margin-top:5px}.oac-avatar{display:grid!important;place-items:center;width:29px;height:29px;border:1px solid var(--line2);background:var(--soft);color:var(--acid)!important;font:900 8px var(--mono)!important}.oac-owner-inner strong{margin:0}.oac-owner-inner small{display:block;margin-top:2px;color:var(--faint);font-size:7px}.oac-blocker{display:flex;gap:8px;align-items:flex-start;margin:8px 0 0;border-left:2px solid var(--red);background:rgba(255,113,106,.045);padding:7px 9px;color:var(--muted);font-size:8px;line-height:1.45}.oac-blocker b{flex:0 0 auto;color:var(--red);font:900 7px var(--mono);text-transform:uppercase}.oac-evidence{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;margin-top:8px;border:1px solid var(--line);background:#0a0e10;padding:9px}.oac-evidence strong{display:block;font-size:8px}.oac-evidence p{margin:3px 0 0;color:var(--faint);font-size:7px;line-height:1.45}.oac-proof{text-align:right}.oac-truth{display:inline-flex;align-items:center;gap:5px;border:1px solid #755e2e;border-radius:999px;padding:3px 6px;color:var(--amber);font:900 6px var(--mono);letter-spacing:.05em;white-space:nowrap}.oac-truth:before{content:"";width:5px;height:5px;border-radius:50%;background:currentColor}.oac-truth.measured{border-color:#2d766e;color:var(--acid)}.oac-truth.unavailable{border-color:#73413e;color:var(--red)}.oac-proof code{display:block;max-width:205px;margin-top:4px;color:var(--faint);font-size:6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.oac-actions{display:flex;justify-content:space-between;gap:9px;align-items:center;margin-top:10px}.oac-deep-link{display:inline-flex;align-items:center;justify-content:center;min-height:46px;border:1px solid #2d776e;background:#0b1c1b;padding:9px 12px;color:var(--acid2);font:900 8px var(--mono);letter-spacing:.03em;text-decoration:none;touch-action:manipulation}.oac-deep-link:after{content:"↗";margin-left:8px}.oac-deep-link:hover{border-color:var(--acid);background:#102523}.oac-deep-link.invalid{border-color:#74403d;color:var(--red);pointer-events:none}.oac-deep-link:focus-visible{outline:3px solid var(--acid);outline-offset:2px}.oac-disabled-controls{display:flex;gap:6px}.oac-action-button{min-height:46px;border:1px solid var(--line2);background:var(--lift);padding:8px 10px;color:var(--faint);font:850 8px var(--mono);cursor:not-allowed;opacity:.62;touch-action:manipulation}.oac-action-button.complete{border-color:#31564f;color:#729c96}.oac-empty{padding:28px;text-align:center}.oac-empty strong{display:block;color:var(--acid);font-size:13px}.oac-empty p{margin:7px auto 0;max-width:480px;color:var(--muted);font-size:9px;line-height:1.5}
  .oac-owner-list{list-style:none;margin:0;padding:5px 14px}.oac-owner-row{display:grid;grid-template-columns:34px minmax(0,1fr) auto;gap:9px;align-items:center;padding:10px 0;border-bottom:1px solid var(--line)}.oac-owner-row:last-child{border-bottom:0}.oac-owner-avatar{display:grid;place-items:center;width:32px;height:32px;border:1px solid var(--line2);background:var(--lift);color:var(--acid);font:900 8px var(--mono)}.oac-owner-row strong{display:block;font-size:9px}.oac-owner-row small{display:block;margin-top:2px;color:var(--faint);font-size:7px}.oac-owner-load{text-align:right;font:900 9px var(--mono)}.oac-owner-load b{display:block;color:var(--red);font:inherit}.oac-owner-load span{display:block;margin-top:2px;color:var(--faint);font:750 7px var(--mono)}
  .oac-ledger{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px;padding:13px}.oac-ledger span{border:1px solid var(--line);background:var(--lift);padding:10px}.oac-ledger small{display:block;color:var(--faint);font:750 7px var(--mono);text-transform:uppercase}.oac-ledger b{display:block;margin-top:5px;font:900 16px var(--mono)}.oac-ledger .hot b{color:var(--red)}.oac-ledger .good b{color:var(--acid)}.oac-ledger-note{margin:0;padding:0 13px 13px;color:var(--muted);font-size:8px;line-height:1.5}.oac-command-lock{margin:0 13px 13px;border:1px solid #70423f;background:rgba(255,113,106,.045);padding:11px}.oac-command-lock span{display:block;color:var(--red);font:900 7px var(--mono);letter-spacing:.08em;text-transform:uppercase}.oac-command-lock strong{display:block;margin:6px 0 4px;font-size:10px}.oac-command-lock p{margin:0;color:var(--muted);font-size:8px;line-height:1.5}
  .oac-truncation{margin:0 24px 22px;border:1px solid #765d2c;background:rgba(255,196,93,.05);padding:11px 13px;color:var(--amber);font-size:9px;line-height:1.5}.oac-footer{display:flex;justify-content:space-between;gap:18px;padding:13px 24px;border-top:1px solid var(--line);background:#080c0d;color:var(--faint);font-size:8px;line-height:1.5}.oac-footer strong{color:var(--muted)}
  @media(max-width:1160px){.oac-source-grid{grid-template-columns:repeat(4,minmax(0,1fr))}.oac-layout{grid-template-columns:1fr}.oac-side{grid-template-columns:repeat(2,minmax(0,1fr))}.oac-side .oac-panel:last-child{grid-column:1/-1}.oac-headlines{grid-template-columns:repeat(3,1fr)}.oac-headline:nth-child(3n){border-right:0}.oac-headline:nth-child(-n+3){border-bottom:1px solid var(--line)}}
  @media(max-width:760px){.oac-hero{grid-template-columns:1fr;padding:30px 20px 24px}.oac-hero h1{font-size:clamp(2.5rem,13vw,4.2rem)}.oac-boundary{grid-template-columns:1fr;padding:12px 20px}.oac-boundary-state{justify-self:start}.oac-pulse{padding:17px 14px 0}.oac-section-head{align-items:flex-start;flex-direction:column}.oac-section-head p{text-align:left}.oac-source-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.oac-layout{padding:14px}.oac-card{grid-template-columns:52px minmax(0,1fr)}.oac-rank{padding-inline:6px}.oac-card-top{flex-direction:column}.oac-sla{align-self:flex-start}.oac-side{grid-template-columns:1fr}.oac-side .oac-panel:last-child{grid-column:auto}.oac-footer{flex-direction:column;padding:13px 14px}}
  @media(max-width:520px){.oac-hero{padding-inline:16px}.oac-headlines{grid-template-columns:repeat(2,1fr)}.oac-headline:nth-child(3n){border-right:1px solid var(--line)}.oac-headline:nth-child(2n){border-right:0}.oac-headline:nth-child(-n+4){border-bottom:1px solid var(--line)}.oac-card{grid-template-columns:1fr}.oac-rank{display:grid;grid-template-columns:auto auto 1fr auto;padding:8px 11px;border-right:0;border-bottom:1px solid var(--line)}.oac-rank-line{width:auto;height:1px;min-height:0}.oac-source-mark{width:30px;height:30px}.oac-card-grid{grid-template-columns:1fr}.oac-evidence{grid-template-columns:1fr}.oac-proof{text-align:left}.oac-actions{align-items:stretch;flex-direction:column}.oac-deep-link{width:100%}.oac-disabled-controls{display:grid;grid-template-columns:1fr 1fr}.oac-action-button{width:100%}.oac-source-grid{grid-template-columns:1fr}.oac-footer{font-size:7px}}
  @media(forced-colors:active){.oac,.oac-panel,.oac-card,.oac-source,.oac-shift,.oac-signal,.oac-owner,.oac-evidence,.oac-deep-link,.oac-action-button{border-color:CanvasText}.oac-source:after,.oac-boundary-mark:before,.oac-truth:before{background:Highlight}.oac-deep-link:focus-visible{outline-color:Highlight}}
  @media(prefers-reduced-motion:reduce){.oac *{scroll-behavior:auto!important;animation:none!important;transition:none!important}}
`;

function safeCount(value: number): string {
  return new Intl.NumberFormat('en-GB').format(Math.max(0, Math.floor(value)));
}

function sourceCard(source: OperatorActionSourceSummaryView, largest: number): string {
  const load = largest === 0 ? 0 : Math.round((source.total / largest) * 100);
  return `<article class="oac-source" style="--load:${load}%" aria-label="${escapeHtml(source.label)}: ${safeCount(source.total)} actions">
    <div class="oac-source-head"><span class="oac-source-code">${escapeHtml(source.code)}</span><strong class="oac-source-total">${safeCount(source.total)}</strong></div>
    <h3>${escapeHtml(source.label)}</h3><p>${safeCount(source.needsNow)} need now · <b>${safeCount(source.breached)} breached</b></p>
  </article>`;
}

function evidenceBlock(action: OperatorActionView): string {
  const reference = action.evidence.evidenceRef ?? 'No evidence reference';
  return `<div class="oac-evidence"><div><strong>Source evidence · ${escapeHtml(action.evidence.label)}</strong><p>${escapeHtml(action.evidence.detail)} · observed ${escapeHtml(action.evidence.observedLabel)}</p></div><div class="oac-proof"><span class="oac-truth ${escapeHtml(action.evidence.truth)}">${escapeHtml(action.evidence.truthLabel)}</span><code title="${escapeHtml(reference)}">${escapeHtml(reference)}</code></div></div>`;
}

function actionCard(action: OperatorActionView): string {
  const person = action.relatedPersonLabel === null ? '' : `<strong class="oac-person">${escapeHtml(action.relatedPersonLabel)}</strong>`;
  const blocker = action.blockedBy === null ? '' : `<p class="oac-blocker"><b>Blocked by</b><span>${escapeHtml(action.blockedBy)}</span></p>`;
  const linkLabel = action.deepLinkValid ? action.deepLinkLabel : 'Evidence link withheld';
  const linkClass = action.deepLinkValid ? '' : ' invalid';
  const linkAria = action.deepLinkValid ? '' : ' aria-disabled="true" tabindex="-1"';
  return `<article class="oac-card ${escapeHtml(action.priority)}" role="listitem" data-action-id="${escapeHtml(action.actionId)}" data-source="${escapeHtml(action.source)}" data-sla-state="${escapeHtml(action.slaState)}" data-needs-now="${String(action.needsNow)}">
    <aside class="oac-rank" aria-label="Queue position ${escapeHtml(action.indexLabel)}"><span class="oac-rank-index">${escapeHtml(action.indexLabel)}</span><span class="oac-source-mark" title="${escapeHtml(action.sourceLabel)}">${escapeHtml(action.sourceCode)}</span><span class="oac-rank-line" aria-hidden="true"></span><span class="oac-priority">${escapeHtml(action.priority.toUpperCase())}</span></aside>
    <div class="oac-card-body"><div class="oac-card-top"><div><div class="oac-eyebrow"><span class="oac-source-label">${escapeHtml(action.sourceLabel)}</span><span class="oac-status ${escapeHtml(action.status)}">${escapeHtml(action.statusLabel)}</span><span>${escapeHtml(action.ageLabel)}</span></div><h3>${escapeHtml(action.title)}</h3><p class="oac-card-detail">${escapeHtml(action.detail)}</p></div><span class="oac-sla ${escapeHtml(action.slaState)}" title="Target ${escapeHtml(action.dueLabel)}">${escapeHtml(action.slaLabel)}</span></div>
      <div class="oac-card-grid"><div class="oac-signal"><span>Why it surfaced</span><strong>${escapeHtml(action.signalLabel)}</strong>${person}</div><div class="oac-owner"><span>Owner</span><div class="oac-owner-inner"><span class="oac-avatar">${escapeHtml(action.ownerInitials)}</span><div><strong>${escapeHtml(action.ownerLabel)}</strong><small>${escapeHtml(action.ownerTeam)}</small></div></div></div></div>${blocker}${evidenceBlock(action)}
      <div class="oac-actions"><a class="oac-deep-link${linkClass}" href="${escapeHtml(action.deepLink)}"${linkAria}>${escapeHtml(linkLabel)}</a><div class="oac-disabled-controls" aria-label="Unavailable action controls"><button class="oac-action-button" type="button" disabled aria-disabled="true" title="Snooze is unavailable because no Operator Action command boundary is connected">Snooze · unavailable</button><button class="oac-action-button complete" type="button" disabled aria-disabled="true" title="Complete is unavailable because no Operator Action command boundary is connected">Complete · unavailable</button></div></div>
    </div>
  </article>`;
}

function actionList(actions: readonly OperatorActionView[], emptyTitle: string, emptyDetail: string): string {
  if (actions.length === 0) {
    return `<div class="oac-empty"><strong>${escapeHtml(emptyTitle)}</strong><p>${escapeHtml(emptyDetail)}</p></div>`;
  }
  return `<div class="oac-list" role="list">${actions.map(actionCard).join('')}</div>`;
}

function ownerRow(owner: OperatorActionOwnerSummaryView): string {
  return `<li class="oac-owner-row"><span class="oac-owner-avatar">${escapeHtml(owner.ownerInitials)}</span><div><strong>${escapeHtml(owner.ownerLabel)}</strong><small>${safeCount(owner.total)} assigned actions</small></div><div class="oac-owner-load"><b>${safeCount(owner.needsNow)} now</b><span>${safeCount(owner.breached)} breached</span></div></li>`;
}

export function renderOperatorActionCentreBody(view: OperatorActionCentreView): string {
  const largestSource = Math.max(0, ...view.sources.map((source) => source.total));
  const sources = view.sources.map((source) => sourceCard(source, largestSource)).join('');
  const owners = view.owners.map(ownerRow).join('');
  const integrityTone = view.integrity.coherent ? ' good' : ' bad';
  const truncation = view.inputTruncated
    ? '<p class="oac-truncation"><strong>Safe queue bound reached.</strong> Additional actions were omitted. Counts are incomplete and the queue requires review before operational use.</p>'
    : '';
  return `<style data-property-predator-operator-action-centre>${OPERATOR_ACTION_CENTRE_STYLE}</style><article class="oac" aria-labelledby="oac-title" data-property-predator-operator-actions data-environment="${escapeHtml(view.environment)}" data-dataset-kind="${escapeHtml(view.datasetKind)}" data-command-boundary="absent" data-mutating-controls="disabled" data-provider-effects="none">
    <header class="oac-hero"><div class="oac-hero-copy"><div class="oac-kicker">Growth HQ · Operator action centre</div><h1 id="oac-title">One queue. <em>Nothing gets dropped.</em></h1><p>Journey stalls, conversion replies, content decisions, webinar readiness, automation failures, connection proofs and CRM tasks—ranked by evidence, urgency and ownership in one decisive operational view.</p></div><aside class="oac-shift" aria-label="Operator shift snapshot"><span>Current operational snapshot</span><strong>${escapeHtml(view.workspaceName)}</strong><p>${escapeHtml(view.asOfLabel)} UTC · ${escapeHtml(view.datasetBoundary)}.</p><div class="oac-shift-meta"><span class="oac-chip env">TEST DATA</span><span class="oac-chip env">FICTIONAL FIXTURE</span><span class="oac-chip${integrityTone}">${escapeHtml(view.integrity.label)}</span><span class="oac-chip">READ + DEEP LINK</span></div></aside></header>
    <section class="oac-boundary" aria-label="Command safety boundary"><span class="oac-boundary-mark">No command boundary</span><p><strong>Evidence links work; mutating controls do not.</strong> Snooze and Complete are visible to prove the intended operator experience, but remain disabled until an audited, workspace-scoped command service is connected.</p><span class="oac-boundary-state">NO SEND · PUBLISH · MUTATION</span></section>
    <section class="oac-headlines" aria-label="Action queue headline metrics"><div class="oac-headline good"><span>Total active</span><strong>${safeCount(view.headline.total)}</strong><small>Bounded operational TEST facts</small></div><div class="oac-headline hot"><span>Need you now</span><strong>${safeCount(view.headline.needsNow)}</strong><small>Breached, due in 2h or blocked P0</small></div><div class="oac-headline hot"><span>SLA breached</span><strong>${safeCount(view.headline.breached)}</strong><small>Sorted to the top automatically</small></div><div class="oac-headline warn"><span>Blocked</span><strong>${safeCount(view.headline.blocked)}</strong><small>Explicit dependency recorded</small></div><div class="oac-headline warn"><span>Unassigned</span><strong>${safeCount(view.headline.unassigned)}</strong><small>Ownership gap stays visible</small></div><div class="oac-headline"><span>Evidence unavailable</span><strong>${safeCount(view.headline.evidenceUnavailable)}</strong><small>Never promoted to fact</small></div></section>
    <section class="oac-pulse" aria-labelledby="oac-pulse-title"><header class="oac-section-head"><div><span>01 · Source pulse</span><h2 id="oac-pulse-title">Seven systems. One operator rhythm.</h2></div><p>Counts expose where the work originates. They do not flatten provenance: every card retains its originating ledger and direct evidence route.</p></header><div class="oac-source-grid">${sources}</div></section>
    <div class="oac-layout"><main class="oac-main">
      <section class="oac-panel" aria-labelledby="oac-now-title"><header class="oac-panel-head"><div><h2 id="oac-now-title">Needs you now</h2><p>Breached SLAs, the next two hours, and blocked critical work. Oldest breaches rise first.</p></div><span class="oac-count hot">${safeCount(view.needsNow.length)} decisive moves</span></header>${actionList(view.needsNow, 'No urgent TEST work', 'Nothing has breached or entered the two-hour action window in this snapshot.')}</section>
      <section class="oac-panel" aria-labelledby="oac-deck-title"><header class="oac-panel-head"><div><h2 id="oac-deck-title">On deck</h2><p>Today, on-track and unscheduled work—still ordered by SLA, priority and stable action key.</p></div><span class="oac-count">${safeCount(view.onDeck.length)} queued</span></header>${actionList(view.onDeck, 'Deck clear', 'No additional operational TEST actions are waiting behind the urgent queue.')}</section>
    </main><aside class="oac-side">
      <section class="oac-panel" aria-labelledby="oac-owner-title"><header class="oac-panel-head"><div><h2 id="oac-owner-title">Ownership load</h2><p>Who carries the queue—and where action is still unowned.</p></div><span class="oac-count">${safeCount(view.owners.length)} owners</span></header><ul class="oac-owner-list">${owners || '<li class="oac-empty"><strong>No owners</strong><p>No TEST work is assigned.</p></li>'}</ul></section>
      <section class="oac-panel" aria-labelledby="oac-ledger-title"><header class="oac-panel-head"><div><h2 id="oac-ledger-title">Queue integrity</h2><p>Unsafe facts stay visible as review work, not operational truth.</p></div><span class="oac-count${view.integrity.coherent ? '' : ' hot'}">${escapeHtml(view.integrity.label)}</span></header><div class="oac-ledger"><span class="good"><small>Valid rows</small><b>${safeCount(view.headline.total - view.integrity.invalidActions)}</b></span><span class="hot"><small>Invalid rows</small><b>${safeCount(view.integrity.invalidActions)}</b></span><span><small>Duplicate keys</small><b>${safeCount(view.integrity.duplicateActions)}</b></span><span><small>Truncated</small><b>${view.inputTruncated ? 'YES' : 'NO'}</b></span></div><p class="oac-ledger-note">${escapeHtml(view.integrity.detail)}</p></section>
      <section class="oac-panel" aria-labelledby="oac-lock-title"><header class="oac-panel-head"><div><h2 id="oac-lock-title">Operational lock</h2><p>The UX is ready for a later command adapter.</p></div><span class="oac-count hot">ABSENT</span></header><div class="oac-command-lock"><span>Deliberate safety rail</span><strong>Snooze and Complete cannot write yet.</strong><p>A future command boundary must prove workspace scope, role, CSRF, idempotency, optimistic concurrency and audit history before either control is enabled.</p></div></section>
    </aside></div>${truncation}<footer class="oac-footer"><span><strong>Operationally shaped TEST data only.</strong> No customer, provider or production system was read or changed.</span><span>${escapeHtml(view.workspaceName)} · ${escapeHtml(view.asOfLabel)} UTC · zero provider effects</span></footer>
  </article>`;
}
