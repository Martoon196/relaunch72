import { escapeHtml } from './ui.js';
import type {
  CampaignApprovalView,
  CampaignChannelPlanView,
  CampaignCommandView,
  CampaignContentVersionView,
  CampaignGateCheckView,
  CampaignMetricView,
  CampaignMilestoneView,
  CampaignOperatorActionView,
} from './campaign-command-presenter.js';

const CAMPAIGN_COMMAND_STYLE = `
  .ccm{--night:#070a0c;--night2:#0d1215;--panel:#11171a;--lift:#151d20;--soft:#1b2428;--line:#263237;--line2:#35454b;--ink:#f5f7f7;--muted:#a6b1b4;--faint:#77868b;--acid:#00e5cc;--acid2:#78ffe9;--amber:#ffca61;--red:#ff7169;--blue:#8db7ff;--display:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;--mono:"SFMono-Regular",Consolas,"Liberation Mono",monospace;position:relative;isolation:isolate;min-width:0;color:var(--ink);background:var(--night);font-family:var(--display);border-radius:0 0 18px 18px;overflow:hidden;box-shadow:0 28px 80px rgba(5,10,12,.25)}
  .ccm *{box-sizing:border-box}.ccm h1,.ccm h2,.ccm h3,.ccm h4,.ccm p{overflow-wrap:anywhere}.ccm code{font-family:var(--mono);overflow-wrap:anywhere}.ccm button{font:inherit}.ccm-visually-hidden{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important}
  .ccm-hero{position:relative;display:grid;grid-template-columns:minmax(0,1fr) minmax(290px,390px);gap:34px;align-items:end;padding:36px 38px 31px;border-bottom:1px solid var(--line);background:radial-gradient(circle at 83% 18%,rgba(0,229,204,.17),transparent 28%),radial-gradient(circle at 5% 106%,rgba(141,183,255,.08),transparent 26%),linear-gradient(135deg,#11181b,#07090b 68%);overflow:hidden}.ccm-hero:before{content:"";position:absolute;width:260px;height:260px;right:31%;top:-190px;border:1px solid rgba(0,229,204,.15);transform:rotate(45deg)}.ccm-kicker{position:relative;display:flex;gap:9px;align-items:center;color:var(--acid);font:900 11px/1.2 var(--mono);letter-spacing:.14em;text-transform:uppercase}.ccm-kicker:before{content:"";width:28px;height:1px;background:var(--acid)}.ccm-hero h1{position:relative;margin:12px 0 13px;max-width:900px;font-size:clamp(2.5rem,5vw,5rem);font-weight:610;line-height:.9;letter-spacing:-.055em}.ccm-hero h1 em{color:var(--acid);font-style:normal}.ccm-hero-copy>p{position:relative;max-width:800px;margin:0;color:var(--muted);font-size:14px;line-height:1.7}.ccm-revision-line{position:relative;display:flex;gap:8px;flex-wrap:wrap;margin-top:18px}.ccm-pill{display:inline-flex;min-height:27px;align-items:center;gap:6px;border:1px solid var(--line2);border-radius:999px;padding:4px 9px;color:var(--muted);font:850 10px/1.2 var(--mono);letter-spacing:.035em}.ccm-pill.good{border-color:#277a70;color:var(--acid2);background:rgba(0,229,204,.04)}.ccm-pill.warn{border-color:#735f34;color:var(--amber)}.ccm-pill.bad{border-color:#70423d;color:var(--red)}
  .ccm-offer-lock{position:relative;border:1px solid rgba(0,229,204,.28);background:rgba(4,9,10,.78);padding:18px 19px;box-shadow:0 18px 55px rgba(0,0,0,.28)}.ccm-offer-lock:before{content:"ONE OFFER";position:absolute;right:12px;top:10px;color:rgba(0,229,204,.35);font:900 9px var(--mono);letter-spacing:.12em}.ccm-offer-lock small{display:block;color:var(--faint);font:850 10px var(--mono);letter-spacing:.09em;text-transform:uppercase}.ccm-offer-lock h2{margin:9px 0 7px;font-size:18px;line-height:1.24}.ccm-offer-lock p{margin:0;color:var(--muted);font-size:11px;line-height:1.55}.ccm-offer-cta{display:flex;justify-content:space-between;gap:12px;margin-top:14px;padding-top:12px;border-top:1px solid var(--line);color:var(--acid);font:850 10px var(--mono);text-transform:uppercase}.ccm-offer-cta code{color:var(--faint);font-size:9px;text-transform:none}
  .ccm-boundary{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:15px;align-items:center;padding:12px 38px;border-bottom:1px solid var(--line);background:#090d0f}.ccm-boundary-mark{color:var(--acid);font:900 10px var(--mono);letter-spacing:.1em;text-transform:uppercase}.ccm-boundary p{margin:0;color:var(--muted);font-size:11px;line-height:1.5}.ccm-boundary p strong{color:var(--ink)}.ccm-boundary-badge{border:1px solid var(--line2);padding:5px 9px;color:var(--faint);font:850 9px var(--mono);letter-spacing:.06em;text-transform:uppercase;white-space:nowrap}
  .ccm-gate{display:grid;grid-template-columns:minmax(310px,1fr) repeat(4,minmax(115px,150px));border-bottom:1px solid var(--line);background:var(--panel)}.ccm-gate-main{position:relative;display:grid;grid-template-columns:64px minmax(0,1fr);gap:15px;align-items:center;padding:18px 22px;border-right:1px solid var(--line)}.ccm-gate-main:before{content:"";position:absolute;left:0;inset-block:0;width:4px;background:var(--red)}.ccm-gate-main.open:before{background:var(--acid)}.ccm-gate-ring{--progress:0deg;width:60px;height:60px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(var(--red) var(--progress),var(--line) 0);position:relative}.ccm-gate-main.open .ccm-gate-ring{background:conic-gradient(var(--acid) var(--progress),var(--line) 0)}.ccm-gate-ring:before{content:"";position:absolute;inset:5px;border-radius:50%;background:var(--panel)}.ccm-gate-ring strong{position:relative;font:900 12px var(--mono)}.ccm-gate-copy span{color:var(--red);font:900 10px var(--mono);letter-spacing:.08em;text-transform:uppercase}.ccm-gate-main.open .ccm-gate-copy span{color:var(--acid)}.ccm-gate-copy h2{margin:5px 0 4px;font-size:17px}.ccm-gate-copy p{margin:0;color:var(--muted);font-size:11px;line-height:1.45}.ccm-gate-stat{display:grid;align-content:center;padding:15px;border-right:1px solid var(--line)}.ccm-gate-stat:last-child{border-right:0}.ccm-gate-stat span{color:var(--faint);font:850 9px var(--mono);letter-spacing:.07em;text-transform:uppercase}.ccm-gate-stat strong{margin-top:6px;font:900 20px var(--mono)}.ccm-gate-stat strong b{color:var(--acid);font:inherit}.ccm-gate-stat small{margin-top:2px;color:var(--muted);font-size:10px}
  .ccm-system{padding:22px 24px 8px}.ccm-section-label{display:flex;justify-content:space-between;gap:15px;align-items:flex-end;margin:0 0 12px}.ccm-section-label span{color:var(--acid);font:900 10px var(--mono);letter-spacing:.09em;text-transform:uppercase}.ccm-section-label h2{margin:4px 0 0;font-size:17px}.ccm-section-label p{max-width:540px;margin:0;color:var(--faint);font-size:11px;line-height:1.5;text-align:right}.ccm-flow{display:grid;grid-template-columns:minmax(230px,.9fr) minmax(440px,2.25fr) minmax(230px,.9fr);gap:10px;align-items:stretch}.ccm-node{position:relative;border:1px solid var(--line);background:var(--panel);min-width:0;padding:16px}.ccm-node:after{content:"→";position:absolute;right:-18px;top:50%;z-index:2;width:26px;height:26px;display:grid;place-items:center;border:1px solid var(--line2);border-radius:50%;background:var(--night);color:var(--acid);font:900 12px var(--mono);transform:translateY(-50%)}.ccm-node:last-child:after{display:none}.ccm-node-eyebrow{display:flex;justify-content:space-between;gap:8px;color:var(--faint);font:850 9px var(--mono);letter-spacing:.07em;text-transform:uppercase}.ccm-node h3{margin:9px 0 6px;font-size:15px}.ccm-node p{margin:0;color:var(--muted);font-size:10px;line-height:1.5}.ccm-node-foot{display:flex;justify-content:space-between;gap:8px;margin-top:14px;padding-top:10px;border-top:1px solid var(--line);color:var(--faint);font:800 9px var(--mono)}.ccm-node.cohort{border-top:3px solid var(--blue)}.ccm-node.offer{border-top:3px solid var(--acid)}.ccm-node.offer h3{color:var(--acid2)}.ccm-journey-node{border-top:3px solid var(--amber)}.ccm-mini-path{display:grid;grid-template-columns:repeat(6,minmax(70px,1fr));gap:5px;margin-top:12px}.ccm-mini-stop{position:relative;min-width:0;padding:9px 7px;border:1px solid var(--line);background:var(--lift)}.ccm-mini-stop:not(:last-child):after{content:"";position:absolute;right:-6px;top:50%;width:6px;border-top:1px solid var(--line2)}.ccm-mini-stop small{display:block;color:var(--amber);font:900 8px var(--mono)}.ccm-mini-stop strong{display:block;margin-top:4px;font-size:9px;line-height:1.3}.ccm-mini-stop span{display:block;margin-top:5px;color:var(--faint);font:750 8px var(--mono)}
  .ccm-grid{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:14px;padding:8px 24px 22px}.ccm-main,.ccm-side{display:grid;gap:14px;align-content:start;min-width:0}.ccm-panel{min-width:0;border:1px solid var(--line);background:var(--panel)}.ccm-panel-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;padding:16px 17px 13px;border-bottom:1px solid var(--line)}.ccm-panel-head h2{margin:0;font-size:15px}.ccm-panel-head p{margin:4px 0 0;color:var(--muted);font-size:10px;line-height:1.45}.ccm-count{border:1px solid var(--line2);padding:4px 7px;color:var(--muted);font:850 9px var(--mono);white-space:nowrap}
  .ccm-targets{padding:13px 16px}.ccm-target{display:grid;grid-template-columns:36px minmax(120px,160px) minmax(80px,1fr) auto;gap:10px;align-items:center;min-height:43px;border-bottom:1px solid var(--line)}.ccm-target:last-child{border-bottom:0}.ccm-target-index{color:var(--faint);font:850 9px var(--mono)}.ccm-target-copy strong{display:block;font-size:10px}.ccm-target-copy span{display:block;margin-top:2px;color:var(--faint);font-size:9px}.ccm-target-track{height:6px;background:var(--soft);overflow:hidden}.ccm-target-fill{display:block;height:100%;background:linear-gradient(90deg,var(--acid),#3b897f)}.ccm-target-count{text-align:right}.ccm-target-count strong{display:block;color:var(--acid2);font:900 13px var(--mono)}.ccm-target-count small{display:block;color:var(--faint);font:800 8px var(--mono);text-transform:uppercase}
  .ccm-channel-table{display:grid}.ccm-channel{display:grid;grid-template-columns:40px minmax(110px,.65fr) minmax(170px,1.25fr) minmax(145px,1fr) 105px;gap:10px;align-items:center;padding:11px 14px;border-bottom:1px solid var(--line)}.ccm-channel:last-child{border-bottom:0}.ccm-channel-code{width:34px;height:34px;display:grid;place-items:center;border:1px solid var(--line2);color:var(--acid);font:900 9px var(--mono);text-transform:uppercase}.ccm-channel-name strong,.ccm-channel-content strong,.ccm-channel-stage strong{display:block;font-size:10px;line-height:1.35}.ccm-channel-name span,.ccm-channel-content span,.ccm-channel-stage span{display:block;margin-top:3px;color:var(--faint);font-size:9px;line-height:1.35}.ccm-channel-state{justify-self:end;text-align:right}.ccm-channel-state b{display:inline-flex;align-items:center;gap:5px;color:var(--acid);font:850 8px var(--mono);letter-spacing:.04em;text-transform:uppercase}.ccm-channel-state b:before{content:"";width:6px;height:6px;border-radius:50%;background:currentColor}.ccm-channel.locked .ccm-channel-state b{color:var(--red)}.ccm-channel-state small{display:block;margin-top:4px;color:var(--faint);font-size:8px}
  .ccm-webinar{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(230px,.75fr)}.ccm-webinar-show{position:relative;padding:21px;border-right:1px solid var(--line);background:radial-gradient(circle at 93% 15%,rgba(255,202,97,.11),transparent 27%),var(--panel)}.ccm-live-tag{display:inline-flex;gap:6px;align-items:center;color:var(--amber);font:900 9px var(--mono);letter-spacing:.09em;text-transform:uppercase}.ccm-live-tag:before{content:"";width:7px;height:7px;border-radius:50%;background:var(--amber);box-shadow:0 0 0 5px rgba(255,202,97,.09)}.ccm-webinar h3{margin:10px 0 7px;font-size:19px;line-height:1.2}.ccm-webinar-show>p{margin:0;color:var(--muted);font-size:11px}.ccm-webinar-meta{display:flex;gap:15px;flex-wrap:wrap;margin-top:16px;padding-top:13px;border-top:1px solid var(--line)}.ccm-webinar-meta span{color:var(--faint);font:800 9px var(--mono)}.ccm-webinar-meta strong{color:var(--ink);font:inherit}.ccm-webinar-map{padding:16px}.ccm-webinar-map>span{color:var(--faint);font:850 9px var(--mono);letter-spacing:.07em;text-transform:uppercase}.ccm-webinar-map ol{list-style:none;margin:11px 0 0;padding:0}.ccm-webinar-map li{position:relative;padding:0 0 13px 23px;color:var(--muted);font-size:10px}.ccm-webinar-map li:last-child{padding-bottom:0}.ccm-webinar-map li:before{content:"";position:absolute;left:2px;top:2px;width:8px;height:8px;border:2px solid var(--acid);border-radius:50%}.ccm-webinar-map li:not(:last-child):after{content:"";position:absolute;left:6px;top:13px;bottom:2px;border-left:1px solid var(--line2)}.ccm-webinar-map li strong{display:block;color:var(--ink);font-size:10px}.ccm-webinar-map li span{display:block;margin-top:2px;color:var(--faint);font-size:9px}
  .ccm-content-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;padding:10px}.ccm-content{border:1px solid var(--line);background:var(--lift);padding:12px}.ccm-content-top{display:flex;justify-content:space-between;gap:8px}.ccm-content-kind{color:var(--faint);font:850 8px var(--mono);letter-spacing:.06em;text-transform:uppercase}.ccm-content-state{color:var(--acid);font:850 8px var(--mono);text-transform:uppercase}.ccm-content.locked .ccm-content-state{color:var(--red)}.ccm-content h3{margin:7px 0 9px;font-size:11px;line-height:1.35}.ccm-content-proof{display:grid;grid-template-columns:1fr auto;gap:5px;padding-top:9px;border-top:1px solid var(--line);color:var(--faint);font:800 8px var(--mono)}.ccm-content-proof strong{color:var(--muted);font:inherit}.ccm-content-channels{display:flex;gap:4px;flex-wrap:wrap;margin-top:8px}.ccm-content-channels span{border:1px solid var(--line2);border-radius:999px;padding:3px 6px;color:var(--muted);font-size:8px}
  .ccm-gate-ledger{padding:6px 14px}.ccm-check{display:grid;grid-template-columns:22px minmax(0,1fr);gap:8px;padding:10px 0;border-bottom:1px solid var(--line)}.ccm-check:last-child{border-bottom:0}.ccm-check-mark{width:19px;height:19px;display:grid;place-items:center;border:1px solid #2a7b70;border-radius:50%;color:var(--acid);font:900 9px var(--mono)}.ccm-check.fail .ccm-check-mark{border-color:#70423d;color:var(--red)}.ccm-check strong{display:block;font-size:10px}.ccm-check p{margin:3px 0 0;color:var(--faint);font-size:9px;line-height:1.45}
  .ccm-approvals{display:grid;grid-template-columns:repeat(5,minmax(95px,1fr));gap:7px;padding:11px}.ccm-approval{border:1px solid var(--line);padding:10px;background:var(--lift)}.ccm-approval strong{display:block;font-size:9px}.ccm-approval span{display:block;margin-top:5px;color:var(--acid);font:850 8px var(--mono);text-transform:uppercase}.ccm-approval.fail span{color:var(--red)}.ccm-approval code{display:block;margin-top:5px;color:var(--faint);font-size:8px}
  .ccm-truth-intro{padding:12px 14px;border-bottom:1px solid var(--line);background:linear-gradient(90deg,rgba(0,229,204,.05),transparent)}.ccm-truth-intro strong{display:block;font-size:10px}.ccm-truth-intro p{margin:4px 0 0;color:var(--muted);font-size:9px;line-height:1.45}.ccm-truth-counts{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.ccm-truth-counts span{border:1px solid var(--line2);padding:4px 6px;font:850 8px var(--mono);text-transform:uppercase}.ccm-metrics{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;padding:9px}.ccm-metric{min-width:0;border:1px solid var(--line);background:var(--lift);padding:11px}.ccm-metric-head{display:flex;justify-content:space-between;gap:7px;align-items:start}.ccm-metric-head small{color:var(--faint);font-size:8px}.ccm-truth-tag{border:1px solid var(--line2);padding:2px 4px;color:var(--faint);font:900 7px var(--mono);letter-spacing:.04em}.ccm-metric.simulated .ccm-truth-tag{border-color:#756235;color:var(--amber)}.ccm-metric.measured .ccm-truth-tag{border-color:#2a7b70;color:var(--acid)}.ccm-metric-value{display:block;margin:8px 0 6px;font:900 18px var(--mono)}.ccm-metric.unavailable .ccm-metric-value{color:var(--faint);font-size:12px}.ccm-metric p{margin:0;color:var(--faint);font-size:8px;line-height:1.4}.ccm-no-measured{margin:0 9px 9px;padding:10px;border:1px dashed var(--line2);color:var(--faint);font-size:9px;text-align:center}
  .ccm-actions{list-style:none;margin:0;padding:4px 14px}.ccm-action{display:grid;grid-template-columns:28px minmax(0,1fr) auto;gap:9px;padding:11px 0;border-bottom:1px solid var(--line)}.ccm-action:last-child{border-bottom:0}.ccm-action-index{width:25px;height:25px;display:grid;place-items:center;border:1px solid var(--line2);color:var(--acid);font:850 8px var(--mono)}.ccm-action strong{display:block;font-size:10px}.ccm-action p{margin:3px 0 0;color:var(--faint);font-size:9px;line-height:1.45}.ccm-action-meta{text-align:right}.ccm-action-meta b{display:block;color:var(--amber);font:850 8px var(--mono);text-transform:uppercase}.ccm-action-meta span{display:block;margin-top:4px;color:var(--faint);font-size:8px}.ccm-action-meta small{display:block;margin-top:3px;color:var(--muted);font-size:8px}
  .ccm-controls{padding:12px 14px;border-top:1px solid var(--line);background:#0b1012}.ccm-control-row{display:grid;grid-template-columns:1fr 1fr;gap:7px}.ccm-control{min-height:46px;border:1px solid var(--line2);background:var(--soft);color:var(--faint);padding:8px 10px;font-weight:800;font-size:9px;cursor:not-allowed}.ccm-control.primary{border-color:#365851;color:#7ca9a2;background:#12201f}.ccm-controls p{margin:8px 0 0;color:var(--faint);font-size:8px;line-height:1.45}.ccm-controls p strong{color:var(--red)}
  .ccm-truncation{margin:0 24px 18px;padding:11px 13px;border:1px solid #755f34;color:var(--amber);font-size:10px}.ccm-footer{display:flex;justify-content:space-between;gap:14px;padding:14px 24px;border-top:1px solid var(--line);background:#06090a;color:var(--faint);font-size:9px}.ccm-footer strong{color:var(--muted)}
  @media(max-width:1180px){.ccm-gate{grid-template-columns:minmax(300px,1fr) repeat(2,minmax(120px,1fr))}.ccm-gate-stat:nth-child(n+4){border-top:1px solid var(--line)}.ccm-grid{grid-template-columns:minmax(0,1fr) 300px}.ccm-flow{grid-template-columns:minmax(200px,.8fr) minmax(360px,1.7fr) minmax(210px,.8fr)}.ccm-mini-path{grid-template-columns:repeat(3,minmax(80px,1fr));row-gap:6px}.ccm-channel{grid-template-columns:40px minmax(100px,.7fr) minmax(150px,1.2fr) 105px}.ccm-channel-stage{display:none}.ccm-approvals{grid-template-columns:repeat(3,minmax(95px,1fr))}}
  @media(max-width:920px){.ccm-hero{grid-template-columns:1fr;padding:28px 22px 24px}.ccm-boundary{grid-template-columns:1fr;padding:12px 22px}.ccm-boundary-badge{justify-self:start}.ccm-flow{display:flex;overflow-x:auto;padding-bottom:7px;scroll-snap-type:x mandatory}.ccm-node{flex:0 0 290px;scroll-snap-align:start}.ccm-node.journey-node{flex-basis:520px}.ccm-grid{grid-template-columns:1fr;padding:8px 14px 18px}.ccm-system{padding:18px 14px 6px}.ccm-side{grid-template-columns:repeat(2,minmax(0,1fr))}.ccm-side .ccm-panel:first-child{grid-column:1/-1}.ccm-channel{grid-template-columns:40px minmax(110px,.7fr) minmax(160px,1.3fr) minmax(120px,1fr) 105px}.ccm-channel-stage{display:block}.ccm-footer{padding:14px}.ccm-truncation{margin-inline:14px}}
  @media(max-width:680px){.ccm-hero h1{font-size:2.75rem}.ccm-gate{grid-template-columns:repeat(2,minmax(0,1fr))}.ccm-gate-main{grid-column:1/-1;border-right:0}.ccm-gate-stat{border-top:1px solid var(--line)}.ccm-gate-stat:nth-child(odd){border-right:0}.ccm-section-label{align-items:flex-start;flex-direction:column}.ccm-section-label p{text-align:left}.ccm-side{grid-template-columns:1fr}.ccm-side .ccm-panel:first-child{grid-column:auto}.ccm-channel{grid-template-columns:38px minmax(0,1fr) auto}.ccm-channel-content,.ccm-channel-stage{grid-column:2/-1}.ccm-channel-state{grid-column:3;grid-row:1}.ccm-webinar{grid-template-columns:1fr}.ccm-webinar-show{border-right:0;border-bottom:1px solid var(--line)}.ccm-content-list,.ccm-metrics{grid-template-columns:1fr}.ccm-approvals{grid-template-columns:repeat(2,minmax(0,1fr))}.ccm-target{grid-template-columns:28px minmax(0,1fr) auto}.ccm-target-track{grid-column:2/-1}.ccm-footer{flex-direction:column}}
  @media(max-width:440px){.ccm-hero{padding:23px 17px 20px}.ccm-hero h1{font-size:2.25rem}.ccm-boundary{padding-inline:17px}.ccm-gate{grid-template-columns:1fr}.ccm-gate-main{grid-template-columns:52px minmax(0,1fr);padding:16px}.ccm-gate-ring{width:50px;height:50px}.ccm-gate-stat,.ccm-gate-stat:nth-child(odd){border-right:0}.ccm-node{flex-basis:270px}.ccm-node.journey-node{flex-basis:470px}.ccm-mini-path{grid-template-columns:repeat(2,minmax(80px,1fr))}.ccm-panel-head{align-items:stretch;flex-direction:column}.ccm-count{align-self:flex-start}.ccm-approvals{grid-template-columns:1fr}.ccm-action{grid-template-columns:28px minmax(0,1fr)}.ccm-action-meta{grid-column:2;text-align:left}.ccm-control-row{grid-template-columns:1fr}}
  @media(forced-colors:active){.ccm,.ccm-node,.ccm-panel,.ccm-content,.ccm-metric,.ccm-offer-lock{forced-color-adjust:auto}.ccm-gate-main:before,.ccm-target-fill{background:CanvasText}.ccm-gate-ring{background:Canvas}}
  @media(prefers-reduced-motion:reduce){.ccm *{scroll-behavior:auto!important;transition:none!important}}
`;

function safeNumber(value: number): string {
  return String(Math.max(0, Math.min(Math.floor(value), 1_000_000_000)));
}

function time(value: string): string {
  return `<time datetime="${escapeHtml(value)}">${escapeHtml(value.replace('T', ' ').replace('.000Z', ' UTC'))}</time>`;
}

function milestoneMini(milestone: CampaignMilestoneView): string {
  return `<div class="ccm-mini-stop"><small>${escapeHtml(milestone.indexLabel)}</small><strong>${escapeHtml(milestone.label)}</strong><span>${escapeHtml(milestone.targetLabel)}</span></div>`;
}

function targetRow(milestone: CampaignMilestoneView): string {
  const value = milestone.targetCount === null ? '—' : milestone.targetCount.toLocaleString('en-GB');
  return `<div class="ccm-target"><span class="ccm-target-index">${escapeHtml(milestone.indexLabel)}</span><div class="ccm-target-copy"><strong>${escapeHtml(milestone.label)}</strong><span>${escapeHtml(milestone.operatorMeaning)}</span></div><div class="ccm-target-track" role="img" aria-label="${escapeHtml(`${milestone.label}: ${milestone.targetLabel}, simulated planning target`)}"><span class="ccm-target-fill" style="width:${safeNumber(milestone.widthPercent)}%"></span></div><div class="ccm-target-count"><strong>${escapeHtml(value)}</strong><small>SIMULATED target</small></div></div>`;
}

function channelRow(channel: CampaignChannelPlanView): string {
  return `<article class="ccm-channel${channel.gatePasses ? '' : ' locked'}" title="${escapeHtml(channel.gateDetail)}"><span class="ccm-channel-code" aria-hidden="true">${escapeHtml(channel.channelCode)}</span><div class="ccm-channel-name"><strong>${escapeHtml(channel.channelLabel)}</strong><span>${escapeHtml(channel.cadenceLabel)}</span></div><div class="ccm-channel-content"><strong>${escapeHtml(channel.contentTitle)}</strong><span>${escapeHtml(channel.objective)}</span></div><div class="ccm-channel-stage"><strong>${escapeHtml(channel.milestoneLabel)}</strong><span>Journey advancement target</span></div><div class="ccm-channel-state"><b>${channel.gatePasses ? 'Rehearsal ready' : 'Locked'}</b><small>${escapeHtml(channel.scheduledLabel)} UTC</small></div></article>`;
}

function contentCard(content: CampaignContentVersionView): string {
  const channels = content.channelLabels.length > 0
    ? content.channelLabels.map((channel) => `<span>${escapeHtml(channel)}</span>`).join('')
    : '<span>Webinar runbook</span>';
  return `<article class="ccm-content${content.gatePasses ? '' : ' locked'}"><div class="ccm-content-top"><span class="ccm-content-kind">${escapeHtml(content.kindLabel)} · v${safeNumber(content.versionNumber)}</span><span class="ccm-content-state">${content.gatePasses ? 'Exact + fresh' : 'Locked'}</span></div><h3>${escapeHtml(content.title)}</h3><div class="ccm-content-proof"><span>Immutable SHA-256</span><strong>${escapeHtml(content.shortHash)}…</strong><span>Approval</span><strong>${content.exactApproval ? 'Exact decision' : 'Mismatch'}</strong><span>Source proof</span><strong>${content.proofFresh ? 'Current' : 'Expired'}</strong></div><div class="ccm-content-channels" aria-label="Linked channel plans">${channels}</div></article>`;
}

function gateCheck(check: CampaignGateCheckView): string {
  return `<div class="ccm-check${check.passed ? '' : ' fail'}"><span class="ccm-check-mark" aria-hidden="true">${check.passed ? '✓' : '!'}</span><div><strong>${escapeHtml(check.label)}</strong><p>${escapeHtml(check.detail)}</p></div></div>`;
}

function approvalCard(approval: CampaignApprovalView): string {
  return `<article class="ccm-approval${approval.gatePasses ? '' : ' fail'}"><strong>${escapeHtml(approval.scopeLabel)}</strong><span>${escapeHtml(approval.statusLabel)}</span><code>${escapeHtml(approval.subjectSha256.slice(0, 10))}…</code></article>`;
}

function metricCard(metric: CampaignMetricView): string {
  return `<article class="ccm-metric ${escapeHtml(metric.truth)}"><div class="ccm-metric-head"><small>${escapeHtml(metric.label)}</small><span class="ccm-truth-tag">${escapeHtml(metric.truthLabel)}</span></div><strong class="ccm-metric-value">${escapeHtml(metric.displayValue)}</strong><p>${escapeHtml(metric.periodLabel)} · ${escapeHtml(metric.detail)}</p></article>`;
}

function actionRow(action: CampaignOperatorActionView): string {
  const due = action.dueAt === null ? action.dueLabel : `${action.dueLabel} UTC`;
  return `<li class="ccm-action"><span class="ccm-action-index" aria-hidden="true">${escapeHtml(action.indexLabel)}</span><div><strong>${escapeHtml(action.label)}</strong><p>${escapeHtml(action.detail)}</p></div><div class="ccm-action-meta"><b>${escapeHtml(action.priorityLabel)}</b><span>${escapeHtml(action.ownerLabel)}</span><small>${escapeHtml(due)}</small></div></li>`;
}

function campaignSystem(view: CampaignCommandView): string {
  const milestones = view.journey.milestones.map(milestoneMini).join('');
  return `<section class="ccm-system" aria-labelledby="ccm-system-title"><header class="ccm-section-label"><div><span>01 · Campaign architecture</span><h2 id="ccm-system-title">One controlled path from person to offer</h2></div><p>This is not a pile of posts. The exact cohort, journey, content and singular offer are locked into one revision.</p></header><div class="ccm-flow" aria-label="Campaign system map"><article class="ccm-node cohort"><div class="ccm-node-eyebrow"><span>CRM cohort</span><span>SIMULATED</span></div><h3>${escapeHtml(view.cohort.label)}</h3><p>${escapeHtml(view.cohort.definition)}</p><footer class="ccm-node-foot"><span>${escapeHtml(view.cohort.countLabel)} fictional people</span><code>${escapeHtml(view.cohort.snapshotShortHash)}…</code></footer></article><article class="ccm-node journey-node"><div class="ccm-node-eyebrow"><span>Journey · ${escapeHtml(view.journey.label)}</span><span>${safeNumber(view.journey.milestones.length)} milestones</span></div><div class="ccm-mini-path">${milestones}</div><footer class="ccm-node-foot"><span>Targets are planning assumptions</span><code>${escapeHtml(view.journey.journeyId)}</code></footer></article><article class="ccm-node offer"><div class="ccm-node-eyebrow"><span>Conversion destination</span><span>ONE OFFER</span></div><h3>${escapeHtml(view.offer.label)}</h3><p>${escapeHtml(view.offer.promise)}</p><footer class="ccm-node-foot"><span>${escapeHtml(view.offer.callToAction)}</span><code>${escapeHtml(view.offer.shortHash)}…</code></footer></article></div></section>`;
}

function truthPanel(view: CampaignCommandView): string {
  const cards = view.metrics.map(metricCard).join('');
  const noMeasured = view.metricTruth.measured === 0
    ? '<p class="ccm-no-measured"><strong>No measured performance is loaded.</strong> Real spend, reach, registrations and revenue stay unavailable until evidence arrives.</p>'
    : '';
  return `<section class="ccm-panel" aria-labelledby="ccm-truth-title"><header class="ccm-panel-head"><div><h2 id="ccm-truth-title">Spend & metric truth</h2><p>Plans and observations never share the same label.</p></div><span class="ccm-count">evidence-aware</span></header><div class="ccm-truth-intro"><strong>There is no live performance hidden in this screen.</strong><p>SIMULATED is a planning assumption. MEASURED requires evidence. UNAVAILABLE means no defensible number exists.</p><div class="ccm-truth-counts"><span>${safeNumber(view.metricTruth.simulated)} simulated</span><span>${safeNumber(view.metricTruth.measured)} measured</span><span>${safeNumber(view.metricTruth.unavailable)} unavailable</span></div></div><div class="ccm-metrics">${cards}</div>${noMeasured}</section>`;
}

function actionsPanel(view: CampaignCommandView): string {
  return `<section class="ccm-panel" aria-labelledby="ccm-actions-title"><header class="ccm-panel-head"><div><h2 id="ccm-actions-title">Next operator actions</h2><p>The shortest route from prepared to proven.</p></div><span class="ccm-count">${safeNumber(view.nextActions.length)} queued</span></header><ol class="ccm-actions">${view.nextActions.map(actionRow).join('')}</ol><div class="ccm-controls" aria-label="Disabled TEST rehearsal controls"><div class="ccm-control-row"><button class="ccm-control primary" type="button" disabled aria-disabled="true">Start TEST rehearsal · unavailable</button><button class="ccm-control" type="button" disabled aria-disabled="true">Activate campaign · unavailable</button></div><p><strong>No command boundary.</strong> These controls cannot save, queue, connect, spend, send, publish, register anyone or call a provider.</p></div></section>`;
}

export function renderCampaignCommandBody(view: CampaignCommandView): string {
  const progress = view.activationGate.total === 0 ? 0 : Math.round((view.activationGate.passed / view.activationGate.total) * 360);
  const gateTone = view.activationGate.open ? 'open' : 'closed';
  const targetRows = view.journey.milestones.map(targetRow).join('');
  const channels = view.channels.map(channelRow).join('');
  const content = view.contentVersions.map(contentCard).join('');
  const checks = view.activationGate.checks.map(gateCheck).join('');
  const approvals = view.approvals.map(approvalCard).join('');
  const truncation = view.inputTruncated
    ? '<p class="ccm-truncation">Campaign input exceeded a safe evaluation bound. The deterministic rehearsal gate fails closed.</p>'
    : '';
  return `<style data-property-predator-campaign-command>${CAMPAIGN_COMMAND_STYLE}</style><article class="ccm" aria-labelledby="ccm-title" data-environment="test" data-provider-effects="none" data-command-boundary="absent"><header class="ccm-hero"><div class="ccm-hero-copy"><div class="ccm-kicker">Property Predator · Campaign Command</div><h1 id="ccm-title">A campaign is a system. <em>Command it.</em></h1><p>${escapeHtml(view.revision.objective)}</p><div class="ccm-revision-line"><span class="ccm-pill good">Immutable revision v${safeNumber(view.revision.revisionNumber)}</span><span class="ccm-pill">SHA ${escapeHtml(view.revision.shortHash)}…</span><span class="ccm-pill warn">TEST rehearsal only</span><span class="ccm-pill">Owned content pack ${escapeHtml(view.contentPack.shortHash)}…</span></div></div><aside class="ccm-offer-lock" aria-label="Single campaign offer"><small>Conversion contract</small><h2>${escapeHtml(view.offer.label)}</h2><p>${escapeHtml(view.offer.promise)}</p><div class="ccm-offer-cta"><span>${escapeHtml(view.offer.callToAction)}</span><code>${escapeHtml(view.offer.shortHash)}…</code></div></aside></header>
    <section class="ccm-boundary" aria-label="TEST safety boundary"><span class="ccm-boundary-mark">TEST Campaign Command</span><p><strong>Zero outbound effects.</strong> Fictional cohort, simulated targets and rehearsal schedules only. No provider, account, customer, ad spend or live performance feed is connected.</p><span class="ccm-boundary-badge">No send · no publish · no spend</span></section>
    <section class="ccm-gate" aria-label="Deterministic campaign activation gate"><div class="ccm-gate-main ${gateTone}"><div class="ccm-gate-ring" style="--progress:${safeNumber(progress)}deg" role="progressbar" aria-valuemin="0" aria-valuemax="${safeNumber(view.activationGate.total)}" aria-valuenow="${safeNumber(view.activationGate.passed)}"><strong>${safeNumber(view.activationGate.passed)}/${safeNumber(view.activationGate.total)}</strong></div><div class="ccm-gate-copy"><span>Deterministic gate · ${escapeHtml(view.activationGate.label)}</span><h2>${escapeHtml(view.activationGate.headline)}</h2><p>${escapeHtml(view.activationGate.detail)}</p></div></div><div class="ccm-gate-stat"><span>CRM cohort</span><strong><b>${escapeHtml(view.cohort.countLabel)}</b></strong><small>fictional TEST people</small></div><div class="ccm-gate-stat"><span>Owned versions</span><strong>${safeNumber(view.contentVersions.length)}</strong><small>exact + approved</small></div><div class="ccm-gate-stat"><span>Channel rails</span><strong>${safeNumber(view.channels.length)}</strong><small>simulated plans</small></div><div class="ccm-gate-stat"><span>Measured metrics</span><strong>${safeNumber(view.metricTruth.measured)}</strong><small>${view.metricTruth.measured === 0 ? 'none loaded' : 'evidence-backed'}</small></div></section>
    ${campaignSystem(view)}
    <div class="ccm-grid"><div class="ccm-main"><section class="ccm-panel" aria-labelledby="ccm-targets-title"><header class="ccm-panel-head"><div><h2 id="ccm-targets-title">Conversion target spine</h2><p>SIMULATED planning targets, deliberately separated from observed results.</p></div><span class="ccm-count">not performance</span></header><div class="ccm-targets">${targetRows}</div></section><section class="ccm-panel" aria-labelledby="ccm-channels-title"><header class="ccm-panel-head"><div><h2 id="ccm-channels-title">Channel choreography</h2><p>Every placement resolves to an owned content version and a journey milestone.</p></div><span class="ccm-count">${safeNumber(view.channels.length)} TEST rails</span></header><div class="ccm-channel-table">${channels}</div></section><section class="ccm-panel" aria-labelledby="ccm-webinar-title"><header class="ccm-panel-head"><div><h2 id="ccm-webinar-title">Webinar conversion room</h2><p>The live teaching moment, mapped as evidence—not vibes.</p></div><span class="ccm-pill ${view.webinar.gatePasses ? 'good' : 'bad'}">${view.webinar.gatePasses ? 'Rehearsal ready' : 'Locked'}</span></header><div class="ccm-webinar"><div class="ccm-webinar-show"><span class="ccm-live-tag">SIMULATED live room</span><h3>${escapeHtml(view.webinar.title)}</h3><p>${escapeHtml(view.webinar.gateDetail)}</p><div class="ccm-webinar-meta"><span>Session <strong>${escapeHtml(view.webinar.sessionLabel)} UTC</strong></span><span>Runtime <strong>${safeNumber(view.webinar.durationMinutes)} minutes</strong></span><span>Runbook <strong>${escapeHtml(view.webinar.shortHash)}…</strong></span></div></div><div class="ccm-webinar-map"><span>Evidence map</span><ol><li><strong>Registration</strong><span>${escapeHtml(view.webinar.registrationMilestoneId)}</span></li><li><strong>Live attendance</strong><span>${escapeHtml(view.webinar.attendanceMilestoneId)}</span></li><li><strong>Replay consumption</strong><span>${escapeHtml(view.webinar.replayMilestoneId)}</span></li><li><strong>Owned runbook</strong><span>${escapeHtml(view.webinar.contentTitle)}</span></li></ol></div></div></section><section class="ccm-panel" aria-labelledby="ccm-content-title"><header class="ccm-panel-head"><div><h2 id="ccm-content-title">Owned content manifest</h2><p>Exact immutable versions from the company content layer.</p></div><span class="ccm-count">pack ${escapeHtml(view.contentPack.shortHash)}…</span></header><div class="ccm-content-list">${content}</div><div class="ccm-approvals" aria-label="Exact campaign approval pack">${approvals}</div></section></div><aside class="ccm-side"><section class="ccm-panel" aria-labelledby="ccm-ledger-title"><header class="ccm-panel-head"><div><h2 id="ccm-ledger-title">Activation ledger</h2><p>Same facts in, same gate result out.</p></div><span class="ccm-count">${safeNumber(view.activationGate.passed)}/${safeNumber(view.activationGate.total)}</span></header><div class="ccm-gate-ledger">${checks}</div></section>${truthPanel(view)}${actionsPanel(view)}</aside></div>${truncation}
    <footer class="ccm-footer"><span><strong>${escapeHtml(view.workspaceName)}</strong> · ${escapeHtml(view.revision.title)} · snapshot ${time(view.asOf)}</span><span>Immutable revision ${escapeHtml(view.revision.revisionId)} · provider effects ${escapeHtml(view.providerEffects)}</span></footer></article>`;
}
