/**
 * Premium, side-effect-free Automation Studio workspace.
 *
 * The canvas is visually arranged but remains a semantic ordered list. Every
 * node, route and gate can be understood without colour, pointer input or the
 * visual connection treatment.
 */
import { escapeHtml } from './ui.js';
import {
  AUTOMATION_STUDIO_ROUTE,
  type AutomationNodeKind,
  type AutomationNodeView,
  type AutomationReadinessCheckView,
  type AutomationStudioView,
} from './automation-studio-presenter.js';

const AUTOMATION_STUDIO_STYLE = `
  .auto{--a-bg:#07090b;--a-panel:#0d1114;--a-rise:#12181c;--a-soft:#090d0f;--a-line:#243036;--a-line2:#3c4b51;--a-ink:#f4f7f6;--a-muted:#a5b1b3;--a-faint:#718286;--a-teal:#00e5cc;--a-teal2:#64f7e7;--a-green:#7cecb3;--a-amber:#f4b84d;--a-red:#ff736b;--a-blue:#8bb5ff;--a-violet:#c6a4ff;min-width:0;overflow:hidden;border:1px solid #020304;background:var(--a-bg);color:var(--a-ink);font-family:var(--sans,ui-sans-serif,system-ui,sans-serif)}
  .auto *{box-sizing:border-box}.auto h1,.auto h2,.auto h3,.auto p{margin-top:0}.auto a{color:inherit;text-decoration:none}.auto button{font:inherit}.auto code{font-family:var(--mono,ui-monospace,monospace);overflow-wrap:anywhere}.auto-visually-hidden{position:absolute!important;width:1px!important;height:1px!important;padding:0!important;margin:-1px!important;overflow:hidden!important;clip:rect(0,0,0,0)!important;white-space:nowrap!important;border:0!important}
  .auto-hero{position:relative;display:grid;grid-template-columns:minmax(0,1fr) minmax(290px,390px);gap:28px;align-items:end;padding:32px;border-bottom:1px solid var(--a-line);background:radial-gradient(circle at 82% 0,rgba(0,229,204,.18),transparent 33%),linear-gradient(132deg,#151e22,#080a0c 65%);overflow:hidden}.auto-hero::after{content:"";position:absolute;right:-86px;top:-170px;width:330px;height:330px;border:1px solid rgba(0,229,204,.16);border-radius:50%;pointer-events:none}.auto-kicker{display:inline-flex;align-items:center;gap:9px;margin-bottom:14px;color:var(--a-teal2);font-size:11px;font-weight:850;letter-spacing:.18em;text-transform:uppercase}.auto-kicker::before{content:"";width:26px;height:2px;background:var(--a-teal)}.auto-hero h1{max-width:840px;margin-bottom:13px;font-size:clamp(34px,5vw,64px);line-height:.94;letter-spacing:-.055em}.auto-hero h1 em{display:block;color:var(--a-teal);font-style:normal}.auto-hero-copy>p{max-width:790px;margin:0;color:var(--a-muted);font-size:15px;line-height:1.65}.auto-flow-stamp{position:relative;z-index:1;padding:20px;border:1px solid rgba(0,229,204,.28);background:rgba(7,10,12,.82);box-shadow:0 18px 60px rgba(0,0,0,.28)}.auto-flow-stamp small{display:block;margin-bottom:8px;color:var(--a-teal);font-size:10px;font-weight:850;letter-spacing:.15em;text-transform:uppercase}.auto-flow-stamp strong{display:block;margin-bottom:8px;font-size:19px;line-height:1.2}.auto-flow-stamp p{margin-bottom:13px;color:var(--a-muted);font-size:12px;line-height:1.55}.auto-stamp-meta{display:flex;flex-wrap:wrap;gap:7px}.auto-stamp-meta span{padding:6px 8px;border:1px solid var(--a-line);background:#090d0f;color:#d5dcdd;font:700 10px var(--mono,ui-monospace,monospace);letter-spacing:.04em;text-transform:uppercase}
  .auto-safety{display:grid;grid-template-columns:auto 1fr auto;gap:16px;align-items:center;padding:13px 30px;border-bottom:1px solid rgba(0,229,204,.24);background:linear-gradient(90deg,rgba(0,229,204,.12),rgba(0,229,204,.025))}.auto-safety-mark{padding:7px 9px;border:1px solid rgba(0,229,204,.42);color:var(--a-teal2);font:850 10px var(--mono,ui-monospace,monospace);letter-spacing:.1em;text-transform:uppercase}.auto-safety p{margin:0;color:#c8d3d3;font-size:12px;line-height:1.5}.auto-safety strong{color:#fff}.auto-safety-badge{color:var(--a-green);font-size:11px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}
  .auto-command{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;align-items:center;padding:20px 30px;border-bottom:1px solid var(--a-line);background:#0a0e10}.auto-command-copy{display:flex;align-items:center;gap:14px;min-width:0}.auto-state{flex:0 0 auto;width:10px;height:10px;border-radius:50%;background:var(--a-amber);box-shadow:0 0 0 5px rgba(244,184,77,.11)}.auto-command-copy strong{display:block;font-size:14px}.auto-command-copy span{display:block;margin-top:3px;color:var(--a-muted);font-size:11px;line-height:1.45}.auto-command-actions{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:8px}.auto-button{min-height:46px;padding:0 14px;border:1px solid var(--a-line2);background:#11171a;color:#dbe2e3;font-size:11px;font-weight:850;letter-spacing:.04em}.auto-button.primary{border-color:rgba(0,229,204,.4);background:rgba(0,229,204,.1);color:var(--a-teal2)}.auto-button.danger{border-color:rgba(255,115,107,.35);color:#ffaaa5}.auto-button:disabled{cursor:not-allowed;opacity:.58}.auto-button:focus-visible,.auto-node-select:focus-visible,.auto-proof:focus-visible{outline:3px solid #fff;outline-offset:3px}
  .auto-metrics{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));border-bottom:1px solid var(--a-line);background:#080b0d}.auto-metric{min-width:0;padding:17px 18px;border-right:1px solid var(--a-line)}.auto-metric:last-child{border-right:0}.auto-metric small{display:block;margin-bottom:7px;color:var(--a-faint);font-size:9px;font-weight:850;letter-spacing:.13em;text-transform:uppercase}.auto-metric strong{display:block;font-size:22px;line-height:1}.auto-metric span{display:block;margin-top:6px;color:var(--a-muted);font-size:10px;line-height:1.35}.auto-metric.ready strong{color:var(--a-green)}
  .auto-builder{display:grid;grid-template-columns:190px minmax(400px,1fr) 300px;min-height:720px;border-bottom:1px solid var(--a-line)}.auto-palette,.auto-inspector{background:var(--a-soft)}.auto-palette{padding:20px 14px;border-right:1px solid var(--a-line)}.auto-inspector{padding:20px;border-left:1px solid var(--a-line)}.auto-rail-title{margin-bottom:14px}.auto-rail-title small{display:block;margin-bottom:5px;color:var(--a-teal);font-size:9px;font-weight:850;letter-spacing:.13em;text-transform:uppercase}.auto-rail-title h2{margin-bottom:4px;font-size:15px}.auto-rail-title p{margin:0;color:var(--a-faint);font-size:10px;line-height:1.45}.auto-palette-list{display:grid;gap:8px}.auto-palette-item{display:grid;grid-template-columns:30px 1fr;gap:9px;align-items:center;width:100%;min-height:50px;padding:8px;border:1px solid var(--a-line);background:#0e1316;color:#e7ecec;text-align:left}.auto-palette-item:disabled{opacity:.72;cursor:not-allowed}.auto-palette-icon{display:grid;place-items:center;width:30px;height:30px;border:1px solid var(--a-line2);background:#090c0e;color:var(--a-teal);font:900 11px var(--mono,ui-monospace,monospace)}.auto-palette-item b{display:block;font-size:11px}.auto-palette-item span:last-child{display:block;margin-top:2px;color:var(--a-faint);font-size:9px;line-height:1.3}.auto-palette-note{margin-top:16px;padding-top:14px;border-top:1px solid var(--a-line);color:var(--a-faint);font-size:10px;line-height:1.5}
  .auto-canvas-shell{position:relative;min-width:0;padding:20px;background:radial-gradient(circle at 50% 0,rgba(0,229,204,.055),transparent 35%),linear-gradient(rgba(255,255,255,.018) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.018) 1px,transparent 1px),#080b0d;background-size:auto,24px 24px,24px 24px}.auto-canvas-head{display:flex;justify-content:space-between;gap:16px;align-items:center;margin-bottom:20px}.auto-canvas-head small{display:block;margin-bottom:4px;color:var(--a-teal);font-size:9px;font-weight:850;letter-spacing:.13em;text-transform:uppercase}.auto-canvas-head h2{margin:0;font-size:16px}.auto-canvas-head span{color:var(--a-faint);font:700 10px var(--mono,ui-monospace,monospace)}.auto-flow{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));grid-auto-rows:minmax(92px,auto);gap:18px 12px;align-items:start;margin:0;padding:0;list-style:none}.auto-node{position:relative;grid-column:var(--auto-col);grid-row:var(--auto-row);min-width:0}.auto-node::before{content:"";position:absolute;left:50%;top:-18px;width:1px;height:18px;background:var(--a-line2)}.auto-node:first-child::before{display:none}.auto-node-card{position:relative;border:1px solid var(--a-line2);background:rgba(14,19,22,.96);box-shadow:0 12px 30px rgba(0,0,0,.28);transition:border-color .16s ease,transform .16s ease}.auto-node-card:hover{border-color:#5c7078;transform:translateY(-1px)}.auto-node-card.selected{border-color:var(--a-teal);box-shadow:0 0 0 2px rgba(0,229,204,.1),0 16px 40px rgba(0,0,0,.35)}.auto-node-top{display:flex;justify-content:space-between;gap:8px;align-items:center;padding:9px 10px;border-bottom:1px solid var(--a-line);background:#0b0f11}.auto-node-kind{display:flex;align-items:center;gap:7px;color:#dbe3e3;font-size:9px;font-weight:850;letter-spacing:.1em;text-transform:uppercase}.auto-node-kind::before{content:"";width:7px;height:7px;border-radius:50%;background:var(--node-accent,var(--a-teal))}.auto-node[data-kind="condition"]{--node-accent:var(--a-blue)}.auto-node[data-kind="guard"]{--node-accent:var(--a-amber)}.auto-node[data-kind="wait"]{--node-accent:var(--a-violet)}.auto-node[data-kind="action"]{--node-accent:var(--a-green)}.auto-node-sequence{color:var(--a-faint);font:800 9px var(--mono,ui-monospace,monospace)}.auto-node-body{padding:12px}.auto-node-body h3{margin-bottom:7px;font-size:13px;line-height:1.25}.auto-node-body p{margin-bottom:10px;color:var(--a-muted);font-size:10px;line-height:1.5}.auto-node-meta{display:flex;flex-wrap:wrap;gap:5px}.auto-node-meta span{padding:5px 6px;border:1px solid var(--a-line);color:var(--a-faint);font-size:8px;font-weight:800;letter-spacing:.05em;text-transform:uppercase}.auto-node-meta .ready{color:var(--a-green)}.auto-node-meta .blocked{color:var(--a-red)}.auto-node-paths{display:flex;flex-wrap:wrap;gap:5px;padding:0 12px 12px}.auto-node-path{padding:5px 7px;border:1px solid rgba(139,181,255,.2);background:rgba(139,181,255,.06);color:#b8cced;font-size:8px;line-height:1.3}.auto-node-path[data-path="yes"]{border-color:rgba(124,236,179,.25);color:var(--a-green)}.auto-node-path[data-path="no"]{border-color:rgba(255,115,107,.25);color:#ffaaa5}.auto-node-select{display:block;min-height:46px;padding:13px 12px;border-top:1px solid var(--a-line);color:var(--a-teal2);font-size:9px;font-weight:850;letter-spacing:.08em;text-align:center;text-transform:uppercase}.auto-node-select:hover{background:rgba(0,229,204,.06)}
  .auto-inspector-stamp{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:15px;padding-bottom:13px;border-bottom:1px solid var(--a-line)}.auto-inspector-stamp small{color:var(--a-teal);font-size:9px;font-weight:850;letter-spacing:.12em;text-transform:uppercase}.auto-inspector-stamp span{color:var(--a-faint);font:700 9px var(--mono,ui-monospace,monospace)}.auto-inspector h2{margin-bottom:8px;font-size:18px;line-height:1.2}.auto-inspector>p{margin-bottom:16px;color:var(--a-muted);font-size:11px;line-height:1.55}.auto-inspector-state{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:17px}.auto-inspector-state div{padding:10px;border:1px solid var(--a-line);background:#0d1214}.auto-inspector-state small{display:block;margin-bottom:5px;color:var(--a-faint);font-size:8px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.auto-inspector-state strong{font-size:10px;line-height:1.35}.auto-config{margin:0;padding:0;list-style:none}.auto-config li{padding:10px 0;border-top:1px solid var(--a-line)}.auto-config small{display:block;margin-bottom:4px;color:var(--a-faint);font-size:8px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.auto-config strong{display:block;color:#dfe5e5;font-size:10px;line-height:1.45}.auto-sensitive-reject{margin-top:12px;padding:10px;border:1px solid rgba(255,115,107,.35);background:rgba(255,115,107,.07);color:#ffc0bd;font-size:10px;line-height:1.45}.auto-route-summary{margin-top:17px;padding-top:14px;border-top:1px solid var(--a-line)}.auto-route-summary h3{margin-bottom:8px;color:var(--a-faint);font-size:9px;letter-spacing:.1em;text-transform:uppercase}.auto-route-summary ul{display:grid;gap:6px;margin:0;padding:0;list-style:none}.auto-route-summary li{color:var(--a-muted);font-size:9px;line-height:1.4}.auto-route-summary b{color:#dfe5e5}
  .auto-evidence{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(340px,.75fr);border-bottom:1px solid var(--a-line)}.auto-panel{min-width:0;padding:26px 30px}.auto-panel+.auto-panel{border-left:1px solid var(--a-line)}.auto-panel-head{display:flex;justify-content:space-between;gap:18px;align-items:end;margin-bottom:18px}.auto-panel-head small{display:block;margin-bottom:6px;color:var(--a-teal);font-size:9px;font-weight:850;letter-spacing:.13em;text-transform:uppercase}.auto-panel-head h2{margin:0;font-size:20px}.auto-panel-head p{max-width:430px;margin:0;color:var(--a-faint);font-size:10px;line-height:1.45;text-align:right}.auto-gate{margin-bottom:15px;padding:16px;border:1px solid var(--a-line2);background:#0c1113}.auto-gate.open{border-color:rgba(124,236,179,.36);background:linear-gradient(135deg,rgba(124,236,179,.09),#0c1113 55%)}.auto-gate.closed{border-color:rgba(255,115,107,.3)}.auto-gate-top{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:8px}.auto-gate-top small{color:var(--a-faint);font-size:8px;font-weight:850;letter-spacing:.11em;text-transform:uppercase}.auto-gate-top b{padding:5px 7px;border:1px solid currentColor;color:var(--a-red);font:900 9px var(--mono,ui-monospace,monospace);letter-spacing:.1em}.auto-gate.open .auto-gate-top b{color:var(--a-green)}.auto-gate h3{margin-bottom:6px;font-size:14px}.auto-gate p{margin:0;color:var(--a-muted);font-size:10px;line-height:1.5}.auto-blockers{display:grid;gap:6px;margin:10px 0 0;padding:0;list-style:none}.auto-blockers li{padding-left:12px;color:#ffc2be;font-size:9px;line-height:1.45}.auto-blockers li::before{content:"×";margin-left:-12px;margin-right:6px;color:var(--a-red)}.auto-checks{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.auto-check{padding:12px;border:1px solid var(--a-line);background:#0b0f11}.auto-check-top{display:flex;justify-content:space-between;gap:9px;margin-bottom:6px}.auto-check small{color:var(--a-faint);font-size:8px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.auto-check b{color:var(--a-green);font:850 8px var(--mono,ui-monospace,monospace);letter-spacing:.08em}.auto-check.fail b{color:var(--a-red)}.auto-check strong{display:block;margin-bottom:5px;font-size:10px}.auto-check p{margin:0;color:var(--a-faint);font-size:9px;line-height:1.45}.auto-proofs{display:grid;gap:8px}.auto-proof{padding:13px;border:1px solid var(--a-line);background:#0b0f11}.auto-proof-top{display:flex;justify-content:space-between;gap:10px;margin-bottom:7px}.auto-proof-top strong{font-size:10px}.auto-proof-top span{color:var(--a-green);font-size:8px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.auto-proof.fail .auto-proof-top span{color:var(--a-red)}.auto-proof p{margin-bottom:8px;color:var(--a-faint);font-size:9px;line-height:1.45}.auto-proof code{display:block;color:#9aacaf;font-size:8px}.auto-proof-time{display:flex;justify-content:space-between;gap:8px;margin-top:7px;color:var(--a-faint);font-size:8px}
  .auto-observability{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(320px,.85fr);border-bottom:1px solid var(--a-line)}.auto-sim{padding:26px 30px}.auto-audit{padding:26px 30px;border-left:1px solid var(--a-line);background:#090d0f}.auto-sim-summary{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:16px}.auto-sim-summary span{padding:7px 9px;border:1px solid var(--a-line);color:var(--a-muted);font-size:9px}.auto-sim-summary b{color:var(--a-green)}.auto-timeline{margin:0;padding:0;list-style:none}.auto-timeline li{position:relative;display:grid;grid-template-columns:32px minmax(0,1fr) auto;gap:12px;padding:0 0 18px}.auto-timeline li:not(:last-child)::after{content:"";position:absolute;left:15px;top:32px;bottom:0;width:1px;background:var(--a-line2)}.auto-time-index{position:relative;z-index:1;display:grid;place-items:center;width:32px;height:32px;border:1px solid rgba(0,229,204,.35);border-radius:50%;background:#0b1113;color:var(--a-teal);font:850 9px var(--mono,ui-monospace,monospace)}.auto-time-copy strong{display:block;margin-bottom:3px;font-size:11px}.auto-time-copy span{display:block;margin-bottom:4px;color:var(--a-green);font-size:8px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.auto-time-copy p{margin:0;color:var(--a-faint);font-size:9px;line-height:1.45}.auto-time-ms{color:var(--a-faint);font:700 8px var(--mono,ui-monospace,monospace)}.auto-audit-list{display:grid;gap:0;margin:0;padding:0;list-style:none}.auto-audit-list li{padding:12px 0;border-top:1px solid var(--a-line)}.auto-audit-list li:first-child{border-top:0;padding-top:0}.auto-audit-list small{display:flex;justify-content:space-between;gap:10px;margin-bottom:5px;color:var(--a-faint);font-size:8px}.auto-audit-list strong{display:block;margin-bottom:5px;font-size:10px}.auto-audit-list p{margin-bottom:6px;color:var(--a-muted);font-size:9px;line-height:1.45}.auto-audit-list code{color:#819397;font-size:8px}
  .auto-footer{display:flex;justify-content:space-between;gap:18px;padding:16px 30px;background:#060809;color:var(--a-faint);font-size:9px;line-height:1.45}.auto-footer strong{color:#d4dddd}
  @media(max-width:1120px){.auto-builder{grid-template-columns:160px minmax(370px,1fr) 260px}.auto-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}.auto-metric:nth-child(3){border-right:0}.auto-metric:nth-child(-n+3){border-bottom:1px solid var(--a-line)}.auto-evidence,.auto-observability{grid-template-columns:1fr}.auto-panel+.auto-panel,.auto-audit{border-left:0;border-top:1px solid var(--a-line)}}
  @media(max-width:820px){.auto-hero{grid-template-columns:1fr;padding:25px 20px}.auto-safety{grid-template-columns:1fr;padding:14px 20px;gap:9px}.auto-command{grid-template-columns:1fr;padding:17px 20px}.auto-command-actions{justify-content:flex-start}.auto-builder{grid-template-columns:1fr;min-height:0}.auto-palette{border-right:0;border-bottom:1px solid var(--a-line)}.auto-palette-list{grid-template-columns:repeat(5,minmax(0,1fr))}.auto-palette-item{display:block;min-height:64px;text-align:center}.auto-palette-icon{margin:0 auto 6px}.auto-palette-item span:last-child{display:none}.auto-inspector{border-left:0;border-top:1px solid var(--a-line)}.auto-canvas-shell{padding:20px}.auto-panel,.auto-sim,.auto-audit{padding:23px 20px}.auto-footer{padding:15px 20px}.auto-flow{grid-template-columns:repeat(3,minmax(0,1fr))}.auto-node{grid-column:min(var(--auto-col),3)}}
  @media(max-width:560px){.auto-hero h1{font-size:38px}.auto-flow-stamp{padding:16px}.auto-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.auto-metric,.auto-metric:nth-child(3){border-right:1px solid var(--a-line);border-bottom:1px solid var(--a-line)}.auto-metric:nth-child(2n){border-right:0}.auto-palette-list{grid-template-columns:repeat(3,minmax(0,1fr))}.auto-canvas-head,.auto-panel-head{align-items:flex-start;flex-direction:column}.auto-panel-head p{text-align:left}.auto-flow{display:block}.auto-node{margin-bottom:16px}.auto-node::before{left:18px}.auto-checks{grid-template-columns:1fr}.auto-timeline li{grid-template-columns:32px minmax(0,1fr)}.auto-time-ms{grid-column:2}.auto-footer{flex-direction:column}.auto-command-actions{display:grid;grid-template-columns:1fr}.auto-button{width:100%}}
  @media(prefers-reduced-motion:reduce){.auto-node-card{transition:none}}
  @media(forced-colors:active){.auto,.auto-node-card,.auto-flow-stamp,.auto-gate,.auto-check,.auto-proof,.auto-button{border-color:CanvasText}.auto-kicker,.auto-node-kind::before,.auto-state{forced-color-adjust:none}.auto-node-card.selected{outline:3px solid Highlight}.auto-button:disabled{color:GrayText}}
`;

const PALETTE: readonly Readonly<{ kind: AutomationNodeKind; code: string; label: string; detail: string }>[] = Object.freeze([
  { kind: 'trigger', code: '01', label: 'Trigger', detail: 'Start from one event' },
  { kind: 'condition', code: 'IF', label: 'Condition', detail: 'Route on safe data' },
  { kind: 'guard', code: 'OK', label: 'Safety gate', detail: 'Consent or approval' },
  { kind: 'wait', code: 'T+', label: 'Wait', detail: 'Respectful timing' },
  { kind: 'action', code: '→', label: 'Action', detail: 'Bounded TEST outcome' },
]);

function safeNumber(value: number): string {
  return Number.isFinite(value) ? Math.max(0, Math.trunc(value)).toLocaleString('en-GB') : '0';
}

function formatInstant(value: string | null): string {
  if (!value) return 'Not recorded';
  const date = new Date(value);
  return Number.isFinite(date.getTime())
    ? new Intl.DateTimeFormat('en-GB', {
        day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit', hour12: false, timeZone: 'UTC',
      }).format(date) + ' UTC'
    : 'Invalid timestamp';
}

function palette(): string {
  return `<aside class="auto-palette" aria-label="Automation node library"><div class="auto-rail-title"><small>Node library</small><h2>Build the logic</h2><p>Visible for composition planning. Mutation is intentionally locked.</p></div><div class="auto-palette-list">${PALETTE.map((item) => `<button class="auto-palette-item" type="button" disabled aria-disabled="true" title="Authenticated editing boundary not connected"><span class="auto-palette-icon" aria-hidden="true">${escapeHtml(item.code)}</span><span><b>${escapeHtml(item.label)}</b><span>${escapeHtml(item.detail)}</span></span></button>`).join('')}</div><p class="auto-palette-note"><strong>Accessible by design.</strong> The canvas is an ordered list, routes are named in text and colour is never the only status signal.</p></aside>`;
}

function nodePaths(node: AutomationNodeView): string {
  if (node.outgoing.length === 0) return '<div class="auto-node-paths"><span class="auto-node-path">End of this path</span></div>';
  return `<div class="auto-node-paths" aria-label="Next paths">${node.outgoing.map((path) => `<span class="auto-node-path" data-path="${path.path}">${escapeHtml(path.label)} → ${escapeHtml(path.nodeTitle)}</span>`).join('')}</div>`;
}

function flowNode(node: AutomationNodeView): string {
  const href = `${AUTOMATION_STUDIO_ROUTE}?node=${encodeURIComponent(node.nodeId)}`;
  return `<li class="auto-node" data-kind="${node.kind}" style="--auto-col:${node.column};--auto-row:${node.row}" id="${escapeHtml(node.anchorId)}"><article class="auto-node-card${node.selected ? ' selected' : ''}"${node.selected ? ' aria-current="step"' : ''}><div class="auto-node-top"><span class="auto-node-kind">${escapeHtml(node.kindLabel)}</span><span class="auto-node-sequence">${String(node.sequence).padStart(2, '0')}</span></div><div class="auto-node-body"><h3>${escapeHtml(node.title)}</h3><p>${escapeHtml(node.detail)}</p><div class="auto-node-meta"><span class="${node.nodeReady ? 'ready' : 'blocked'}">${node.nodeReady ? 'Configured' : 'Blocked'}</span><span>${escapeHtml(node.effectLabel)}</span><span>${node.providerMode === 'simulated' ? 'Simulated rail' : 'No provider'}</span></div></div>${nodePaths(node)}<a class="auto-node-select" href="${escapeHtml(href)}" aria-label="Inspect ${escapeHtml(node.title)}">Inspect step</a></article></li>`;
}

function canvas(view: AutomationStudioView): string {
  return `<section class="auto-canvas-shell" aria-labelledby="auto-canvas-title"><header class="auto-canvas-head"><div><small>Visual flow canvas</small><h2 id="auto-canvas-title">Trigger → decisions → protected actions</h2></div><span>${safeNumber(view.nodes.length)} nodes · v${safeNumber(view.version)}</span></header><ol class="auto-flow" aria-label="Accessible automation flow in execution order">${view.nodes.map(flowNode).join('')}</ol></section>`;
}

function inspector(view: AutomationStudioView): string {
  const node = view.selectedNode;
  const config = node.configuration.length > 0
    ? `<ul class="auto-config" aria-label="Selected node configuration">${node.configuration.map((entry) => `<li><small>${escapeHtml(entry.label)}</small><strong>${escapeHtml(entry.value)}</strong></li>`).join('')}</ul>`
    : '<p>No display-safe settings are available for this step.</p>';
  const rejected = node.rejectedConfigurationCount > 0
    ? `<p class="auto-sensitive-reject"><strong>${safeNumber(node.rejectedConfigurationCount)} sensitive or oversized setting${node.rejectedConfigurationCount === 1 ? '' : 's'} rejected.</strong> Credential-shaped fields are never rendered by Automation Studio.</p>`
    : '';
  const routes = [
    ...node.incoming.map((route) => `<li><b>From ${escapeHtml(route.nodeTitle)}:</b> ${escapeHtml(route.label)}</li>`),
    ...node.outgoing.map((route) => `<li><b>To ${escapeHtml(route.nodeTitle)}:</b> ${escapeHtml(route.label)}</li>`),
  ];
  return `<aside class="auto-inspector" aria-labelledby="auto-inspector-title"><div class="auto-inspector-stamp"><small>Step inspector</small><span>${String(node.sequence).padStart(2, '0')} / ${safeNumber(view.nodes.length)}</span></div><h2 id="auto-inspector-title">${escapeHtml(node.title)}</h2><p>${escapeHtml(node.detail)}</p><div class="auto-inspector-state"><div><small>Type</small><strong>${escapeHtml(node.kindLabel)}</strong></div><div><small>Effect</small><strong>${escapeHtml(node.effectLabel)}</strong></div><div><small>Provider</small><strong>${node.providerMode === 'simulated' ? 'SIMULATED' : 'NONE'}</strong></div><div><small>Readiness</small><strong>${node.nodeReady ? 'Configured' : 'Blocked'}</strong></div></div>${config}${rejected}<section class="auto-route-summary" aria-label="Selected node routes"><h3>Named connections</h3><ul>${routes.length > 0 ? routes.join('') : '<li>No incoming or outgoing route is declared.</li>'}</ul></section></aside>`;
}

function checkCard(check: AutomationReadinessCheckView): string {
  return `<article class="auto-check${check.passed ? '' : ' fail'}"><div class="auto-check-top"><small>${escapeHtml(check.category)}</small><b>${check.passed ? 'PASS' : 'BLOCK'}</b></div><strong>${escapeHtml(check.label)}</strong><p>${escapeHtml(check.detail)}</p></article>`;
}

function gatesAndChecks(view: AutomationStudioView): string {
  const blockers = view.testActivationGate.blockers.length > 0
    ? `<ul class="auto-blockers">${view.testActivationGate.blockers.map((blocker) => `<li>${escapeHtml(blocker)}</li>`).join('')}</ul>`
    : '';
  return `<section class="auto-panel" aria-labelledby="auto-readiness-title"><header class="auto-panel-head"><div><small>Deterministic readiness</small><h2 id="auto-readiness-title">Activation gates</h2></div><p>Passing a gate proves eligibility only. With no command boundary, it cannot change runtime state.</p></header><article class="auto-gate ${view.testActivationGate.open ? 'open' : 'closed'}"><div class="auto-gate-top"><small>TEST activation gate</small><b>${view.testActivationGate.label}</b></div><h3>${escapeHtml(view.testActivationGate.headline)}</h3><p>${escapeHtml(view.testActivationGate.detail)}</p>${blockers}</article><article class="auto-gate closed"><div class="auto-gate-top"><small>LIVE activation gate</small><b>${view.liveActivationGate.label}</b></div><h3>${escapeHtml(view.liveActivationGate.headline)}</h3><p>${escapeHtml(view.liveActivationGate.detail)}</p></article><div class="auto-checks" aria-label="Automation readiness checks">${view.readinessChecks.map(checkCard).join('')}</div></section>`;
}

function proofPanel(view: AutomationStudioView): string {
  const proofHtml = view.readinessProofs.length > 0
    ? view.readinessProofs.map((proof) => `<article class="auto-proof${proof.passes ? '' : ' fail'}" tabindex="0"><div class="auto-proof-top"><strong>${escapeHtml(proof.label)}</strong><span>${escapeHtml(proof.stateLabel)}</span></div><p>${escapeHtml(proof.detail)}</p><code>${escapeHtml(proof.evidenceRef ?? 'No evidence reference')}</code><div class="auto-proof-time"><span>Checked ${escapeHtml(formatInstant(proof.verifiedAt))}</span><span>Expires ${escapeHtml(formatInstant(proof.expiresAt))}</span></div></article>`).join('')
    : '<p>No readiness proof was supplied. Activation fails closed.</p>';
  return `<section class="auto-panel" aria-labelledby="auto-proof-title"><header class="auto-panel-head"><div><small>Immutable evidence</small><h2 id="auto-proof-title">Readiness proof</h2></div></header><div class="auto-proofs">${proofHtml}</div></section>`;
}

function simulationPanel(view: AutomationStudioView): string {
  return `<section class="auto-sim" aria-labelledby="auto-sim-title"><header class="auto-panel-head"><div><small>Deterministic rehearsal</small><h2 id="auto-sim-title">Latest zero-effect simulation</h2></div><p>No wait was scheduled, no provider was called and no contact was touched.</p></header><div class="auto-sim-summary"><span>Status <b>${escapeHtml(view.simulation.stateLabel)}</b></span><span>Trigger ${escapeHtml(view.simulation.triggerLabel)}</span><span>Provider effects <b>0</b></span><span>Contact effects <b>0</b></span></div><ol class="auto-timeline" aria-label="Simulation execution transcript">${view.simulation.steps.map((step) => `<li><span class="auto-time-index">${String(step.sequence).padStart(2, '0')}</span><div class="auto-time-copy"><strong>${escapeHtml(step.nodeTitle)}</strong><span>${escapeHtml(step.outcomeLabel)}</span><p>${escapeHtml(step.detail)}</p></div><span class="auto-time-ms">${safeNumber(step.durationMs)} ms</span></li>`).join('')}</ol></section>`;
}

function auditPanel(view: AutomationStudioView): string {
  return `<aside class="auto-audit" aria-labelledby="auto-audit-title"><header class="auto-panel-head"><div><small>Change history</small><h2 id="auto-audit-title">Audit trail</h2></div></header><ol class="auto-audit-list">${view.audit.map((event) => `<li><small><span>${escapeHtml(formatInstant(event.occurredAt))}</span><span>${escapeHtml(event.actorLabel)}</span></small><strong>${escapeHtml(event.eventLabel)}</strong><p>${escapeHtml(event.detail)}</p><code>${escapeHtml(event.evidenceRef)}</code></li>`).join('')}</ol></aside>`;
}

export function renderAutomationStudioBody(view: AutomationStudioView): string {
  const boundedMessage = view.inputTruncated
    ? 'Safety bounds were applied. TEST activation remains closed.'
    : 'All automation inputs fit the bounded evaluation window.';
  return `<style data-property-predator-automation-studio>${AUTOMATION_STUDIO_STYLE}</style><article class="auto" aria-labelledby="auto-title" data-provider-effects="none" data-contact-effects="none" data-command-boundary="absent"><header class="auto-hero"><div class="auto-hero-copy"><span class="auto-kicker">Property Predator · Automation Studio</span><h1 id="auto-title">Build the logic. <em>Prove the safety.</em></h1><p>A visual command room for turning lead behaviour into precise next actions — with consent, approval, simulation and audit evidence visible before a flow can move.</p></div><aside class="auto-flow-stamp"><small>Selected TEST flow</small><strong>${escapeHtml(view.name)}</strong><p>${escapeHtml(view.summary)}</p><div class="auto-stamp-meta"><span>v${safeNumber(view.version)}</span><span>${escapeHtml(view.environmentLabel)}</span><span>${escapeHtml(view.runtimeLabel)}</span></div></aside></header><section class="auto-safety" aria-label="TEST-only automation boundary"><span class="auto-safety-mark">TEST automation room</span><p><strong>Visual planning and deterministic evidence only.</strong> This slice cannot activate a flow, schedule work, call a provider, send a message or touch a real contact.</p><span class="auto-safety-badge">Zero live effects</span></section><section class="auto-command" aria-label="Automation runtime controls"><div class="auto-command-copy"><span class="auto-state" aria-hidden="true"></span><div><strong>${escapeHtml(view.runtimeLabel)} · activation gate ${view.testActivationGate.label}</strong><span>Controls stay protected until an authenticated command boundary is deliberately integrated and tested.</span></div></div><div class="auto-command-actions"><button class="auto-button" type="button" disabled aria-disabled="true">Run TEST simulation · unavailable</button><button class="auto-button danger" type="button" disabled aria-disabled="true">Pause TEST · unavailable</button><button class="auto-button primary" type="button" disabled aria-disabled="true">Activate TEST · unavailable</button></div></section><section class="auto-metrics" aria-label="Automation summary"><article class="auto-metric"><small>Flow nodes</small><strong>${safeNumber(view.metrics.nodes)}</strong><span>Accessible visual steps</span></article><article class="auto-metric"><small>Actions</small><strong>${safeNumber(view.metrics.actions)}</strong><span>TEST or draft only</span></article><article class="auto-metric ready"><small>Readiness</small><strong>${safeNumber(view.metrics.checksPassed)}/${safeNumber(view.metrics.checksTotal)}</strong><span>Deterministic checks</span></article><article class="auto-metric ready"><small>Proof</small><strong>${safeNumber(view.metrics.proofPassed)}/${safeNumber(view.metrics.proofTotal)}</strong><span>Current evidence</span></article><article class="auto-metric"><small>Simulation</small><strong>${safeNumber(view.metrics.simulatedSteps)}</strong><span>Transcript steps</span></article><article class="auto-metric"><small>Outbound effects</small><strong>0</strong><span>Provider + contact</span></article></section><section class="auto-builder" aria-label="Visual automation builder">${palette()}${canvas(view)}${inspector(view)}</section><section class="auto-evidence">${gatesAndChecks(view)}${proofPanel(view)}</section><section class="auto-observability">${simulationPanel(view)}${auditPanel(view)}</section><footer class="auto-footer"><span><strong>${escapeHtml(view.workspaceName)}</strong> · snapshot ${escapeHtml(view.asOf)} · TEST presentation only</span><span>${escapeHtml(boundedMessage)}</span></footer></article>`;
}
